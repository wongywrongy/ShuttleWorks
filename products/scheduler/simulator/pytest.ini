[pytest]
; Own rootdir on purpose: the product suite's rootdir is products/scheduler
; with testpaths=["tests"], so nothing under simulator/ ever enters the CI
; PR gate. Run these with:  cd products/scheduler/simulator && pytest
testpaths = tests
addopts = -q
