fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
ortools>=9.8.0
python-multipart>=0.0.6
sqlalchemy>=2.0.0
alembic>=1.13.0
# Postgres driver — SQLAlchemy auto-selects when DATABASE_URL starts
# with ``postgresql://`` (or ``postgresql+psycopg2://``). The pure-binary
# wheel keeps the Dockerfile compiler-free.
psycopg2-binary>=2.9.0
# Supabase Python client — used by ``app/dependencies.get_current_user``
# to verify the JWT on every protected route. When ``SUPABASE_URL`` is
# blank the dependency short-circuits into local-dev mode and the client
# is never instantiated.
supabase>=2.0.0

