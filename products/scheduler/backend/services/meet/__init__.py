"""Meet-kind pure computations that sit above the tournament state blob.

Currently just the pool-standings computation (``standings.py``); grows
alongside future Meet-only server-side derivations.
"""
