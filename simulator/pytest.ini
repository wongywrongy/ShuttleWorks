[pytest]
; Own rootdir on purpose: the backend suite's rootdir is the repo root
; with testpaths=["tests"], so nothing under simulator/ ever enters the CI
; PR gate. Run these with:  cd simulator && pytest
testpaths = tests
addopts = -q
