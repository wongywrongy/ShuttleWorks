{
  "name": "cp-sat-scheduler-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:entrant-evidence": "playwright test tests/10-entrant-r11-evidence.spec.ts",
    "test:bracket-geometry": "playwright test --config playwright.geometry.config.ts",
    "test:console-contracts": "playwright test tests/console-browser-contracts.spec.ts",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.9.0"
  }
}
