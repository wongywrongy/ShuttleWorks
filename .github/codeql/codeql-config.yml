# Vendored third-party skill content is maintained upstream.
# All application, package, tooling, infrastructure, and test code stays in scope.
name: "ShuttleWorks CodeQL config"

paths-ignore:
  - ".github/skills/**"
