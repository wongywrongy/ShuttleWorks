[
  { "shape": "MatchStateDTO", "field": "actualCourtId", "side": "hand-only", "kind": "violation", "why": "F-DM-28b - 19 hand fields against the wire's 10; dropped by the backend StrictIgnoringModel, so a write carrying it is silently discarded. Live-ops state has two half-authorities. Closing this is P1/P4 work, not P0." },
  { "shape": "MatchStateDTO", "field": "actualSlotId", "side": "hand-only", "kind": "violation", "why": "F-DM-28b - 19 hand fields against the wire's 10; dropped by the backend StrictIgnoringModel, so a write carrying it is silently discarded. Live-ops state has two half-authorities. Closing this is P1/P4 work, not P0." },
  { "shape": "MatchStateDTO", "field": "delayed", "side": "hand-only", "kind": "violation", "why": "F-DM-28b - 19 hand fields against the wire's 10; dropped by the backend StrictIgnoringModel, so a write carrying it is silently discarded. Live-ops state has two half-authorities. Closing this is P1/P4 work, not P0." },
  { "shape": "MatchStateDTO", "field": "delayReason", "side": "hand-only", "kind": "violation", "why": "F-DM-28b - 19 hand fields against the wire's 10; dropped by the backend StrictIgnoringModel, so a write carrying it is silently discarded. Live-ops state has two half-authorities. Closing this is P1/P4 work, not P0." },
  { "shape": "MatchStateDTO", "field": "delayedPlayerId", "side": "hand-only", "kind": "violation", "why": "F-DM-28b - 19 hand fields against the wire's 10; dropped by the backend StrictIgnoringModel, so a write carrying it is silently discarded. Live-ops state has two half-authorities. Closing this is P1/P4 work, not P0." },
  { "shape": "MatchStateDTO", "field": "postponed", "side": "hand-only", "kind": "violation", "why": "F-DM-28b - dropped by the backend StrictIgnoringModel and hand-preserved across every write in hooks/useLiveTracking.ts:269-276 (one of only TWO fields that merge is applied to). Live-ops state has two half-authorities. Closing this is P1/P4 work, not P0." },
  { "shape": "MatchStateDTO", "field": "pinned", "side": "hand-only", "kind": "violation", "why": "F-DM-28b - 19 hand fields against the wire's 10; dropped by the backend StrictIgnoringModel, so a write carrying it is silently discarded. Live-ops state has two half-authorities. Closing this is P1/P4 work, not P0." },
  { "shape": "MatchStateDTO", "field": "playerConfirmations", "side": "hand-only", "kind": "violation", "why": "F-DM-28b - dropped by the backend StrictIgnoringModel and hand-preserved across every write in hooks/useLiveTracking.ts:269-276 (one of only TWO fields that merge is applied to). Live-ops state has two half-authorities. Closing this is P1/P4 work, not P0." },
  { "shape": "MatchStateDTO", "field": "sets", "side": "hand-only", "kind": "violation", "why": "F-DM-28b - 19 hand fields against the wire's 10; dropped by the backend StrictIgnoringModel, so a write carrying it is silently discarded. Live-ops state has two half-authorities. Closing this is P1/P4 work, not P0." },
  { "shape": "RosterGroupDTO", "field": "type", "side": "hand-only", "kind": "violation", "why": "F-DM-29 class - client-side roster tree field absent from the wire shape. Delete from dto.ts, or model it on the wire, to close." },
  { "shape": "RosterGroupDTO", "field": "parentId", "side": "hand-only", "kind": "violation", "why": "F-DM-29 class - client-side roster tree field absent from the wire shape. Delete from dto.ts, or model it on the wire, to close." },
  { "shape": "RosterGroupDTO", "field": "children", "side": "hand-only", "kind": "violation", "why": "F-DM-29 class - client-side roster tree field absent from the wire shape. Delete from dto.ts, or model it on the wire, to close." },
  { "shape": "RosterGroupDTO", "field": "playerIds", "side": "hand-only", "kind": "violation", "why": "F-DM-29 class - client-side roster tree field absent from the wire shape. Delete from dto.ts, or model it on the wire, to close." },
  { "shape": "PlayerDTO", "field": "status", "side": "hand-only", "kind": "violation", "why": "F-DM-28a - refused by the backend StrictModel (extra=forbid); a write carrying it would 422. The one read (modules/operations/run/MeetMatchPanel.tsx:70, p.status !== 'withdrawn') is a permanent no-op. Delete from dto.ts to close." },
  { "shape": "PlayerDTO", "field": "withdrawalReason", "side": "hand-only", "kind": "violation", "why": "F-DM-28a - refused by the backend StrictModel (extra=forbid); a write carrying it would 422. Delete from dto.ts to close." },
  { "shape": "PlayerDTO", "field": "withdrawnAt", "side": "hand-only", "kind": "violation", "why": "F-DM-28a - refused by the backend StrictModel (extra=forbid); a write carrying it would 422. Delete from dto.ts to close." },
  { "shape": "MatchDTO", "field": "eventCode", "side": "hand-only", "kind": "violation", "why": "F-DM-29 class - hand field absent from the wire shape. Delete from dto.ts, or model it on the wire, to close." },
  { "shape": "TournamentStateDTO", "field": "recoveredFromBackup", "side": "hand-only", "kind": "violation", "why": "F-DM-29 class - hand field absent from the wire shape. Delete from dto.ts, or model it on the wire, to close." },
  { "shape": "InviteCreatedDTO", "field": "email", "side": "hand-only", "kind": "violation", "why": "F-DM-29 class - hand field absent from the wire shape. Delete from dto.ts, or model it on the wire, to close." }
]
