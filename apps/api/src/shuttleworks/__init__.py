"""Named ShuttleWorks deployment entry points.

The package is deliberately separate from the legacy ``core`` modules: it is
the stable boundary for profile-specific composition while the modular
monolith is migrated incrementally.
"""
