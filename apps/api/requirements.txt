fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
# Exact pin: CP-SAT results are only reproducible within a single solver
# version, and determinism is a product guarantee (SP-CLOUD-1 Rule 5e).
# Bumping this requires re-running the determinism e2e test and accepting
# that historical schedules may not re-solve byte-identically.
ortools==9.15.6755
python-multipart>=0.0.6
sqlalchemy>=2.0.0
alembic>=1.13.0
# Postgres driver — psycopg 3. ``database/session.py`` normalises bare
# ``postgresql://`` URLs to ``postgresql+psycopg://`` so SQLAlchemy picks
# this driver (its default for that scheme is still psycopg2). The
# ``binary`` extra keeps the Dockerfile compiler-free.
psycopg[binary]>=3.2
# Argon2id password hashing (RFC 9106 / OWASP-recommended KDF) for the
# self-hosted account system (SP-CLOUD-2).
argon2-cffi>=23.1

# Optional-at-runtime application telemetry.  These stay on one coordinated
# OpenTelemetry release because the contrib instrumentations deliberately use
# beta versioning.  The application imports none of them unless an OTLP
# endpoint is explicitly configured (see core/telemetry/bootstrap.py).
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-sqlalchemy==0.65b0
opentelemetry-instrumentation-urllib==0.65b0
opentelemetry-instrumentation-logging==0.65b0
