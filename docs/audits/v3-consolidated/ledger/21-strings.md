# Ledger — package 21 (public discovery, overview, draw index, directory)

Columns per plan §4: string key/file:line · surface · state · current text · verdict · final text · factual prerequisite · finding IDs · evidence.

## V3-PE01.1 — Discovery intro sentence

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `discovery.tsx` season intro `<p>` | `/e/` season view | `filters.view !== 'completed'` | "Explore Badminton tournaments, schedules, and published results through ShuttleWorks." | change | "Find badminton tournaments, schedules, and results." | One short sentence, ordinary capitalization, no platform-name reference; the `<meta name="description">` keeps the longer platform sentence | V3-PE01.1 | `apps/entrant/app/routes/discovery.tsx` |

## V3-PE01.2 — Discovery deadline

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `SeasonStatusCell.tsx` `chip-open` cell | Discovery calendar row, entries open | `closesAt` present | "Entries open · closes in 15d" | change | "Closes 30 Aug 2026, 13:00 GMT+1 · 5d" (exact tournament-timezone instant primary, relative days secondary) | New `SeasonRowDTO.closesAt`/`timeZone` (backend) carry the exact instant; `formatMomentInZone` (D11, the entrant time authority) renders it | V3-PE01.2 | `apps/api/src/entries/entries_json.py` `SeasonRowDTO`, `apps/entrant/app/components/SeasonStatusCell.tsx` |
| same cell | Discovery calendar row, entries open, no parseable exact instant | any | (unchanged) | keep | "Entries open · closes in Nd" (fallback, unchanged copy) | Rule 4: degrade to what is known rather than omit the countdown entirely | V3-PE01.2 | `openDeadlineLabel` fallback branch |

## V3-PE01.3 — Discovery locality

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `SeasonCalendar.tsx` `CalendarRow` meta line | Discovery calendar row | venue address parses to 2+ comma segments | "Kingsway Centre" (venue only) | change | "Kingsway Centre · London, United Kingdom" | New `SeasonRowDTO.locality`, a best-effort "City, Country" line out of the free-text venue address (no structured city/country column exists — D2) | V3-PE01.3 | `apps/api/src/entries/entries_json.py` `_locality`, `apps/entrant/app/components/SeasonCalendar.tsx` |
| same | Discovery calendar row | address has no comma / is absent | (unchanged) | keep | venue name only — no invented locality | Rule 4: omit, never guess | V3-PE01.3 | `_locality` returns `None` |

## V3-PE02.1 — Footer brand line

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `PlayShell.tsx` footer `<p>` | every public page | any | "ShuttleWorks · tournament entries · by Yunavero" | change | "ShuttleWorks by Yunavero" | `BRAND_SIGNATURE` (`packages/brand/generated.ts`) already exists as the shared canonical string; PlayShell now renders it instead of assembling its own | V3-PE02.1 | `apps/entrant/app/components/PlayShell.tsx` |

## V3-PE03.1 — Overview introduction repetition

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `tournament.tsx` `OverviewPanel` intro `<p>` | Tournament overview | `page.introText` set | (organizer/system-authored text, rendered verbatim — no change to the render site) | no-op in this package's file scope; **debt logged** | n/a | `page.page.introText` is a plain organizer-authored field (`EntryPage.intro_text`, `PATCH /entries/{id}` sets it) — this route already renders it VERBATIM with no system-added repetition around it. The observed repetitive paragraph (title/state/prize/venue/five draw sizes crammed into one sentence) is generated by `simulator/tournament_sim/seed.py`'s demo-data seeding, out of this package's file scope (backend DTOs + entrant routes only) | V3-PE03.1 | `apps/api/src/entries/entries_json.py` `PageDTO.introText`; `simulator/tournament_sim/seed.py:2074` builds the offending sentence for demo/simulator fixtures |

## V3-PE03.2 — Overview registration aggregate

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `tournament.tsx` `OverviewPanel` stat row | Tournament overview | entries closed or zero registered | "Event registrations 0" | cut | (row omitted entirely) | 0 registrations is a different, unrelated fact from a published draw's participant count — printing it invited the "broken tournament" misread the finding names | V3-PE03.2 | `apps/entrant/app/routes/tournament.tsx` |
| same | Tournament overview | entries open, ≥1 registered | "Event registrations 12" | change | "Entered so far 12" | Answers an entry question only while entries can still be entered | V3-PE03.2 | same |

## V3-PE03.3 — Overview hero lead

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `HeroHeader.tsx` subtitle | Tournament hero, live phase | `phase === 'live'` | "Entries closed" (binary chip, alongside a "Follow live matches" CTA) | change | "Live now" | New `statusOverride` prop: when the server ships an explicit lifecycle `phase`, the subtitle leads with `phaseLabel(phase)` instead of the binary entries chip; entry closure remains reachable in the Key dates section (`timelineModel`'s "Entries close" row, unchanged) | V3-PE03.3 | `apps/entrant/app/components/HeroHeader.tsx`, `apps/entrant/app/routes/tournament.tsx` |

## V3-PE04.1 — Event label casing (entrant tier, canonical source)

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| new: `apps/entrant/app/lib/eventLabels.ts` `EVENT_LABEL_MAP` | every public list/legend/filter/heading in this package's scope | n/a | "Mens Singles" / "Mens Doubles" / "Womens Singles" / "Womens Doubles" (EventRow's `displayEventName`, driven off freeform `discipline` text) | add + change | `{ MS: "Men's singles", WS: "Women's singles", MD: "Men's doubles", WD: "Women's doubles", XD: 'Mixed doubles' }` — one map, consumed by `EventRow.tsx`'s row heading + aria-label and `EntrantsList.tsx`'s per-row aria-label | Package 14's console mirror (`apps/console/src/platform/domain/eventLabels.ts`) already expects and pins this exact map via a disk-read parity test — this file satisfies that contract | V3-PE04.1 | `apps/entrant/app/lib/eventLabels.ts`; `apps/console/src/platform/domain/__tests__/eventLabels.test.ts` (passes against this file unmodified) |

## V3-PE04.2 — Draw index counts

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `EventRow.tsx` count column | Draw index / Draws tab, draw published | any | "0 confirmed registrations · 32 draw participants" | change | "32 pairs" (doubles) / "32 players" (singles) — the draw's own participant count, alone | The draw's participant count supersedes the registration count once a draw exists; the two are different, gated sources and must never render side by side | V3-PE04.2 | `apps/entrant/app/components/EventRow.tsx`; unit from the pre-existing `entryCountLabel` (`lib/draws.types.ts`) |
| same | Draw index / Draws tab, no draw yet | any | "7 confirmed registrations" | change | "7 players" / "4 pairs" | Same one-count, one-unit rule applies before a draw exists | V3-PE04.2 | same |
| `tournament.tsx` `DrawsPanel` column header | Draw index / Draws tab | any | "Registrations / draw participants" | change | "Entered" | One column, one meaning, matching the single-count body cells above | V3-PE04.2 | `apps/entrant/app/routes/tournament.tsx` |

## V3-PE04.3 — Draw index action + duplicated publication message

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `EventRow.tsx` draw button | Draw index / Draws tab, draw published | any | "Draw" | change | "View draw" | Outcome-named action | V3-PE04.3 | `apps/entrant/app/components/EventRow.tsx` |
| `EventRow.tsx` state column | Draw index / Draws tab, draw published, no results yet | any | "Draw published" | cut | (blank — the action already states availability) | Removes the duplicated publication message beside the "View draw" button; the roundless-draw fact line ("Draw published · rounds to be scheduled") is untouched since it is a distinct, non-duplicated fact | V3-PE04.3 | same |

## V3-PE05.1 — Directory A–Z jump index

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `EntrantsList.tsx` `Events:` legend paragraph | Player directory | `eventLabels` non-empty (old API) | "Events: MS Men's Singles · WD Women's Doubles · …" | cut | (removed — the `eventLabels` prop is removed entirely) | Replaced by the jump index below; the per-row event codes remain, now with a canonical full-name `aria-label` instead of the removed inline legend | V3-PE05.1 | `apps/entrant/app/components/EntrantsList.tsx` |
| `EntrantsList.tsx` new `<nav aria-label="Jump to letter">` | Player directory, 2+ letter groups | any | (did not exist) | add | One link per PRESENT letter, `href="#dir-<letter>"`, plain anchors to the existing `data-letter-group` sections (now carrying matching `id`s) | Works with no JS and on mobile; never links a letter with no section | V3-PE05.1 | same |

## V3-PE05.2 — Directory search label and count

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `entrants-filter.js` `boot()` count paragraph | Player directory, JS enabled | any | A second "253 players" count minted beside the input (duplicating the SSR count above it) | cut | (removed — `apply()` now updates the ONE SSR-rendered `[data-search-count]` node in place) | One count, updated live | V3-PE05.2 | `apps/entrant/public/assets/entrants-filter.js`; `apps/entrant/app/components/EntrantsList.tsx` |
| `entrants-filter.js` search input | Player directory, JS enabled | any | placeholder-only "Filter by name or club", no visible label | change | Persistent visible `<label>` "Find a player" (or "Find an entrant") + placeholder "Name or club" | The label survives a typed query; a placeholder alone does not | V3-PE05.2 | `apps/entrant/public/assets/entrants-filter.js` `findLabel` |
