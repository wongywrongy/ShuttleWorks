# Ledger — package 16 (venue-board publishing)

Columns per plan §4: string key/file:line · surface · state · current text · verdict · final text · factual prerequisite · finding IDs · evidence.

## Venue board configuration ("Publish → Displays")

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `DisplayConfig.tsx:81` page heading | Publish → Displays | static | `"Display"` (`<h2>`) | change | `"Venue board"` | One name for the board everywhere on this page (ruling 1). | V3-OC22.2 | `apps/console/src/modules/workspace/DisplayConfig.tsx` |
| `DisplayConfig.tsx` Preview section | Publish → Displays | had a link | Inline `<Section title="Preview">` with a 30rem `<iframe src={publicUrl}>` and a "Preview unavailable until a public display link is ready." fallback | cut (behavior), change (copy) | Section kept, but body is a single `Row` with a "Preview fullscreen" `<a target="_blank">` action to the SAME minted `publicUrl` — never an embedded frame | Plan §3 "Remove the tiny board preview instead of widening it" (Adopt, supersedes V3-OC22.1); acceptance requires an explicit "Preview fullscreen" action, tested against the configured token and a working return path. | V3-OC22.1 | `apps/console/src/modules/workspace/DisplayConfig.tsx`; test: `DisplayConfig.test.tsx` "targets the minted ?token= capability URL…", "never renders an inline preview iframe…" |
| `DisplayConfig.tsx` no-token state | Publish → Displays | conditional | `"No public link yet. Only a workspace owner can create one. Ask an owner to share it from Publish displays."` (self-referential link back to the same page) | change | `"No venue board link yet. Only a workspace owner can create one."` (denied state, no dead self-link) plus, on a genuine load failure, `"The venue board link could not be loaded."` + a **Retry** action that re-attempts the mint (the same `get_or_create` call — functionally "create") | Backend `GET /tournaments/{id}/display-token` is owner-gated (`require_tournament_access("owner")`) and auto-creates on first successful call — a true "does not exist yet" state is only ever the denied case for a non-owner; a genuine fetch error is the only case an on-page action can retry. | V3-OC22.1 | `apps/api/src/display/display.py` `get_or_create_display_token`; `DisplayConfig.tsx`; tests "shows the reason and no action…", "shows a reason and a create/retry action…" |
| `DisplayConfig.tsx` layout | Publish → Displays | structural | Two-column grid: Board sources + Public link (left), Preview iframe (right, sticky) | change | Single column: Board sources, then Preview action, then Board layout (Meet only) — board-source rows first, one explicit preview action, per plan §3 acceptance | Acceptance: "board-source rows above… fullscreen action remains adjacent" (superseded treatment; applied to the new single-column order instead of a wider two-up layout). | V3-OC22.1 | `apps/console/src/modules/workspace/DisplayConfig.tsx` |
| `DisplayConfig.tsx` `showLinkControls` prop | Publish → Displays / dead direct route | structural | Prop toggled whether `DisplayConfig` rendered its OWN "Public link" section (Copy/Open/hint) alongside the fullscreen `<SharingTab scope="links">` composition, duplicating link controls on the canonical page | cut | Prop removed; `DisplayConfig` never owns link controls — `SharingTab` (scope="links") is the one and only surface for Copy/Replace of the board link | Ruling 1: one board name, one link-control owner, no duplicated headings/controls on the composed page. | V3-OC22.2 | `apps/console/src/modules/workspace/DisplayConfig.tsx`, `WorkspaceShellSurface.tsx` |
| `displayConfig/DisplayPreview.tsx` (whole file) | dead code — never mounted (confirmed: no import site outside its own file/doc-comments) | — | A scaled 480×230 `CourtsView` swatch over a fixed sample-players/scores fixture, captioned "Sample data" | cut | File deleted | Ruling 2: "remove… the sample-data mock as the default view." It was not even wired as a default view — a leftover from before the real live iframe replaced it — so removing it is a straight dead-code cut, not a behavior change. | V3-OC22.1 (plan §3 "sample-data mock") | `git grep` confirms no import site before deletion; `DisplayLayoutEditor.tsx` doc-comment updated to note the removal |

## Sharing → Venue board link ("Publish → Displays", composed under `DisplayConfig`)

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `SharingTab.tsx` scope="links" heading/intro | Publish → Displays | static, rendered unconditionally | `<h2>Display link</h2>` + `"Manage view-only display links and revoke access deliberately."` | cut (this scope only) | No heading/intro rendered for scope="links" — `DisplayConfig`'s own "Venue board" heading, directly above this component on the composed page, is the page's one title | V3-OC22.2's acceptance: "no repeated 'public display link' headings"; the composed page previously showed "Displays" (tab) → "Display" (`DisplayConfig` h2) → "Display link" (this h2) → "PUBLIC DISPLAY LINK" (eyebrow) → "Public display link" (field label ×2) on one screen. | V3-OC22.2 | `SharingTab.tsx`; test "scope=\"links\" renders no page heading of its own" |
| `SharingTab.tsx:249` eyebrow | Publish → Displays | static | `"PUBLIC DISPLAY LINK"` | change | `"BOARD LINK"` | Same consolidation — avoids repeating the "public display link" phrase a third time on the page. | V3-OC22.2 | `SharingTab.tsx` |
| `SharingTab.tsx:251` access sentence | Publish → Displays | static | `"Anyone with this link can view the read-only venue display: no sign-in required."` | change | `"Anyone with this link can view the board."` | Ruling 3's exact required sentence. | V3-OC22.2 | `SharingTab.tsx` |
| `SharingTab.tsx:254,259` field label/aria | Publish → Displays | static | `"Public display link"` (visible label + duplicate `aria-label`) | change | `"Venue board link"` | Ruling 1: one board name everywhere, including the field the operator actually reads/copies. | V3-OC22.2 | `SharingTab.tsx`; `SharingTab.test.tsx` (`getByLabelText('Venue board link')`, ~9 call sites updated) |
| `SharingTab.tsx` Replace button label/aria | Publish → Displays | static, two-state | Resting: `"Revoke and replace link"` / aria `"Revoke and replace the public display link"`. Armed: `"Confirm: revoke and replace"` / aria `"Confirm revoking and replacing the public display link"` | change | Resting: `"Replace link"` / aria `"Replace the venue board link"`. Armed: `"Confirm: replace link"` / aria `"Confirm replacing the venue board link"` | Ruling 3: "Copy/Replace/Revoke are each one action" — this control performs replace-by-rotation as one action; naming it "Replace" (not "Revoke and replace") matches what the operator is choosing to do, and drops the second board name. | V3-OC22.2 | `SharingTab.tsx`; `SharingTab.test.tsx` |
| `SharingTab.tsx` Replace consequence text | Publish → Displays | conditional on armed state | Resting: `"Revokes the current link immediately and issues a new one."` Armed: `"Every venue display goes blank until you re-share the new link. Press Escape to cancel."` | change | Always shown, adjacent to the control: `"Replacing the link stops the old link from working."` (armed state appends `" Every venue display goes blank until you re-share the new one. Press Escape to cancel."` — the same fact plus the confirm/cancel affordance, not a different claim) | Ruling 3's exact required sentence, stated next to the control rather than only once armed. | V3-OC22.2 | `SharingTab.tsx`; test "states the replace consequence next to the control, at rest" |
| `SharingTab.tsx` rotate-failure error | Publish → Displays | conditional (on failure) | `"The display link could not be replaced. Retry when connected."` | change | `"The venue board link could not be replaced. Retry when connected."` | Same board name across success/failure copy. | — (consistency, not a routed finding) | `SharingTab.tsx` |
| `SharingTab.tsx` load-failure banner | Publish → Displays | conditional (on failure) | `"The display link could not be loaded."` / button `"Retry display link"` | change | `"The board link could not be loaded."` / button `"Retry board link"` | Same board name across states. | — (consistency) | `SharingTab.tsx` |
| `SharingTab.tsx` scope="all" intro | legacy consolidated Sharing tab (`ws-sharing`) | static | `"Public links and collaborator access are separate. Share each deliberately."` | change | `"Public links and collaborator access are separate."` | Ruling 3: "no 'deliberately'." Scope-general, not board-specific, but the word reads as the same admonition tone the finding flags; dropping it costs nothing (the sentence is still accurate without it). | V3-OC22.2 (extended for consistency) | `SharingTab.tsx`; test asserts `queryByText(/deliberately/i)` is null |

## Operator-side preview entry (`DisplayProduct.tsx`, the Display module's own Preview tab — package 17 owns the public board renderers, not this operator chrome)

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `DisplayProduct.tsx` no-link banner | Display module → Preview tab | conditional | `"A workspace owner can enable the venue display link in Publish."` | change | `"A workspace owner can enable the venue board link in Publish."` | One board name, applied to the sibling surface that already uses the correct full-size embed pattern (out of scope to remove — it is not "tiny," it is the module's actual full-size live preview). | — (consistency) | `apps/console/src/modules/display/DisplayProduct.tsx` |

## Verify-only (no change needed)

- `DisplayProduct.tsx`'s embedded `<iframe>` (the Display module's own Preview tab, `/tournaments/{id}/tv`) was inspected and left as-is: it renders at `absolute inset-0 h-full w-full` (full viewport, not "tiny"), already has its own working "Open fullscreen" new-tab action next to it, and is explicitly out of this package's scope per the task brief ("the operator-side preview entry only — not the public board renderers"). The narrow-preview finding (V3-OC22.1) is about the **configuration page's** iframe, not this one.

## Debt logged (not fixed here — see `docs/reference/debt-log.md`)

- **V3-16-1:** `WorkspaceShellSurface.tsx`'s `case 'display-config':` branch renders `<DisplayConfig tid={tid} modules={modules} />` alone (no `SharingTab`), reached only when `segment === 'display-config'` AND the URL does NOT contain `/publish/` — but the only nav entry that ever sets that segment is `publish/displays`, which is intercepted by the earlier `location.pathname.includes('/publish/')` check before the switch runs. The branch is dead code (confirmed by grep: no other nav item maps to `display-config`). Not deleted here — it is a pre-existing structural leftover outside this package's file-level scope (`WorkspaceShellSurface.tsx`'s routing logic, not its Displays composition), and removing a switch case is a different kind of change than the copy/structure work this package was scoped for.
