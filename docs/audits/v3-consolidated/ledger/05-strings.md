# Ledger — package 05 (false claims)

Columns per plan §4: string key/file:line · surface · state · current text · verdict · final text · factual prerequisite · finding IDs · evidence.

## A1 — Staff privacy

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `SetupProduct.tsx:404-409` (people paragraph) | Setup → Staff contacts | static | "Contacts are stored for operator coordination. The current public site does not display staff details; the Public checkbox preserves publication intent for a future public projection. Email remains a separate operator field and is never selected by this checkbox." | change | "Staff contacts are visible to tournament operators and are never published." | No public serializer reads `contacts` (`entries_public.py`, `entries_site.py`, `display/display.py`) | V3-OC12.1 | grep confirmed no public read path for `contacts`; verified before edit |
| `SetupProduct.tsx:417` (Public column) | Setup → Staff contacts table | static | `{ field: 'public', label: 'Public', type: 'checkbox' }` column | cut | column removed | Column controlled a DB field (`Contact.public`) no serializer ever reads | V3-OC12.1 | same as above; backend field/DTO left untouched, logged as debt |
| `apps/api/src/workspaces/setup.py:260` (`_IMPACT["people"]`) | Setup save confirmation / downstream-impact panel | static | `["operator contacts", "public contact details"]` | change | `["operator contacts"]` | Same non-publication fact | V3-OC12.1 | `DownstreamImpact.tsx` renders this list verbatim; no public consumer exists |

## A2 — Module disabling

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `ModuleCatalogRow.tsx` `module-impact-{id}` (ownsData=true) | Settings → Modules row | module enabled, has data | "Owns operational data; existing matches or draws are preserved. Review them before disabling." | change | "{Name} has draws or matches, so it stays on. It can be turned off once they are removed through {Name}." | `_module_has_data` (meet→matches, bracket→bracket_events) really blocks disable with 409 `MODULE_HAS_DATA`; DTO only exposes a boolean `hasData`, not counts, so counts are not claimed | V3-OC26.1 | `apps/api/src/workspaces/workspace_modules.py:95-219`, `core/schemas.py` `WorkspaceModuleDTO` |
| `ModuleCatalogRow.tsx` `module-impact-{id}` (ownsData=false) | Settings → Modules row | module has no data | "Display settings do not own match data." / "No operational records are stored yet." | change | "Turning {Name} off hides it from this workspace's navigation. Nothing is stored yet, so nothing is deleted." | Disabling a data-free module is a pure status flip (`repo.modules.update`); nothing is ever deleted by disable | V3-OC26.1 | same route file, PATCH handler body |
| `ModuleCatalogRow.tsx` `module-completion-{id}` (status disabled) | Settings → Modules row | disabled | "Off; existing data preserved" | change | "Off" | A disabled module with data cannot exist per the has-data guard (data blocks disabling); the phrase implied a state that can't occur, and duplicated the impact line | V3-OC26.1 | same |
| `ModuleCatalogRow.tsx` modal body (`Review {name} data impact`) | Settings → Modules → Review impact modal | opened from a has-data row | "This module owns operational data in this workspace. Disabling it could hide or invalidate those records, so the server keeps it enabled until the data is cleared through its normal workflow. No data will be removed here." | change | "{Name} has draws or matches, so it stays on. It can be turned off once they are removed through {Name}." (same sentence as the row) | Same as above — modal must state the same consequence as the row (ruling) | V3-OC26.1 | same |
| `ModulesSettingsTab.tsx` `blockedReason` (hasData branch) | Settings → Modules row (title attr / extra reason line) | module enabled, has data | "This module has matches or draws. Clear its data before disabling it." | cut | (removed — row's impact line already states the consequence; button branch is "Review impact", not a disabled "Disable" button, so this reason was redundant and instructed deletion-to-disable) | Ruling: never instruct the user to delete data in order to disable | V3-OC26.1 | `ModuleCatalogRow.tsx` render branches: `ownsData` renders "Review impact", not "Disable" |

## A3 — Local saves / sync / backups

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `SyncBackupsTab.tsx:177` primary line | Settings → Sync & Backups | authority.status present | "Sync is active" / "Sync needs attention" (derived from `authority.state`) | change | "Saved on this device." (state-independent) | Authority epoch state ≠ transfer/sync state; console writes never reach the sync outbox (`sync/operation_log.py`, no console call site) | (map A3) | maps/05-06 A3; `sync/reconciliation.py`, `sync/agent.py` opt-in requirements |
| `SyncBackupsTab.tsx:180` | Settings → Sync & Backups | static | "Changes made here are saved locally and reconciled with the cloud when connected." | cut | (removed) | Same — no console write path ever populates the outbox | (map A3) | same |
| `SyncBackupsTab.tsx:185` "Cloud copy up to date" | Settings → Sync & Backups | `pending_operations === 0` (always true w/ empty outbox) | "Cloud copy up to date" shown whenever pending is 0, including a workspace that never synced | change | Shown only when `acknowledged_operations > 0` (and no pending/blocked), labeled "Synced" | New DTO field `acknowledged_operations` (count of outbox rows ever acknowledged) added so the claim has real evidence | (map A3) | `apps/api/src/sync/reconciliation.py::latest_authority_status` (new query), `sync/schemas.py::AuthorityStatus.acknowledged_operations` |
| `SyncBackupsTab.tsx:188` "{n} committed locally · awaiting cloud" | Settings → Sync & Backups | `pending_operations > 0` | unchanged wording, gating changed | keep (rebadged "Syncing · …") | Shown only when `pending_operations > 0`; true whenever it fires (real outbox count) | (map A3) | same |
| new: "Needs attention" row | Settings → Sync & Backups | `blocked_operations > 0` | (did not exist as a distinct row before; old copy conflated blocked into the binary "needs attention" title) | conditional/new | "Needs attention" shown only when `blocked_operations > 0` | Real permanently-blocked outbox rows | (map A3) | `sync/reconciliation.py` blocked query |

## A4 — Entrant account copy (email delivery)

| string key/file:line | surface | state | current text | verdict | final text | factual prerequisite | finding IDs | evidence |
|---|---|---|---|---|---|---|---|---|
| `identity/entrants_routes.py:515-531 _mail` | backend mail wrapper | n/a | swallowed all exceptions, returned `None` | change | returns `bool` (`True`=sent, `False`=failed); still never raises | `core/email.py::send_email` raises only on SMTP failure; console backend (log-only) never raises → counts as "accepted" per R4 | (map A4) | `core/email.py:60-95`, `core/config.py:302` |
| `identity/entrants_routes.py::_send_verification` | backend | n/a | returned `None` | change | returns `bool`, propagated from `_mail` | same | (map A4) | — |
| `resend_verification` → `/e/verify/sent` | verify.tsx "sent" state | resend succeeded | "A fresh confirmation link has been sent to your account email…" | keep (success branch only) | unchanged, now gated on `!mailFailed` | Session-gated route is not enumeration-sensitive (R10 binds only signup/reset-request), so it may report the real outcome | (map A4) | `entrants_routes.py::resend_verification`; R10 scope confirmed by route docstrings |
| `resend_verification` → `/e/verify/sent?ok=0` (new) | verify.tsx "sent" state | resend failed | (did not exist) | new | "We could not send the email. Try again in a moment, or contact the organizer." + resend action ("Try again") | `_send_verification` now reports failure; carried via `?ok=0` query on the redirect (safe: this route always redirects to the same page regardless of address validity — no enumeration axis exists here to protect) | (map A4) | `verify.tsx` loader/`mailFailed`; `apps/entrant/tests/recovery.render.test.ts` new case |
| `resetPassword.tsx:108` "We will email you a link…" | Reset request form (pre-submit) | static | unchanged | keep | unchanged | Describes the action about to be taken, not a completed outcome; not a success claim | (map A4) | R4 exception: request page copy unchanged |
| `resetPassword.tsx:147-150` "…It is good for one hour." | Reset "sent" page | static hardcoded | "a reset link is on its way. It is good for one hour." | change | TTL now sourced from `?ttlMinutes=` on the backend redirect (same value on every request, found or not — no enumeration risk) via `formatTtl()`; sentence omitted if the param is absent | `settings.reset_token_ttl_minutes` carried via redirect query, not a new endpoint, per ruling | (map A4) | `entrants_routes.py::request_entrant_password_reset` now appends `ttlMinutes`; `resetPassword.tsx` `formatTtl` |
| `enter.tsx:333` partner-invite hint | Entry form, doubles partner field | pre-submission | "We email them an invitation. Nothing is entered in their name until they accept, and you can leave this blank and add them later." | change | "We'll try to email them an invitation. Nothing is entered in their name until they accept, and you can leave this blank and add them later." | `_send_partner_invite` can fail; there is no post-submission entrant-facing surface in this package's file scope (receipt page, out of scope) to report per-invite failure, and no resend/edit-entry route exists — see debt log entry | (map A4) | `entries_json.py::_send_partner_invite` now returns bool; `partner_routes.py`/`entries_me.py` confirm no invite-resend path exists |
| `entries_json.py::_send_partner_invite` | backend mail wrapper | n/a | returned `None`, swallowed exceptions | change | returns `bool`; behavior at call site unchanged (redirect is deliberately outcome-independent, see adjacent `result.replayed` comment) | R4 | debt-log entry added: "Partner-invite delivery failure has no entrant-facing recovery path" |
| `login.tsx:271-274` "Your entrant account is ready." | `/e/login/created` | static | unchanged | keep | unchanged | Reached by an identical 303 on both created/existing branches — deliberately ambiguous, true on both | V3 map A4 (no change) | `entrants_routes.py:653-660` |

## A5 — Preview / import / editable-later

No changes. Verified true against the map: `NewWorkspacePage.tsx:213-214` (Setup remains editable unless `authority === 'domain'`, `SetupProduct.tsx:627`), roster import (`RosterTab.tsx:722-770`, real), Display preview (`DisplayProduct.tsx` live iframe real; `DisplayPreview.tsx` sample data correctly labelled). No "editable in Plan" claim exists anywhere in the audited scope.
