# Public visual fixes — agent implementation checklist

**Status:** implemented and reviewed against the canonical source fixture on 2026-09-08. See [current review and evidence](../../surface-book-remediation/canonical-review.md); historical acceptance notes below retain their original context.\
**Repository:** [wongywrongy/ShuttleWorks](https://github.com/wongywrongy/ShuttleWorks)\
**Source baseline:** `main` at `997a6911e99f7019f2e566bb2b6d0a2378b3a9d0`, checked on 2026-09-07.\
**Input:** Public entrant site critique — surface book 2026-09-07, supplied as Pasted markdown(1).md.\
**Scope:** public discovery, tournament pages, players/profiles, schedule, draws, and regulations.

## 1. Instructions to the implementing agent

Implement the requested direction in every package below. As with operator-visual-fixes.md, the critique's recommendations are intentional product changes. Existing contracts, defaults, or tests that require the old presentation must be updated as part of implementation. Do not turn requested work into optional proposals or ask for the same design approval again.

Follow the packages in order. Implement, verify, capture evidence, then check completion. An existing feature counts as complete only when the actual user journey works with representative data. Document a genuine blocker precisely and continue unblocked work; do not silently omit requirements.

Preserve complete server-rendered HTML, native links/forms, no-JavaScript fallbacks, page-scoped progressive scripts, and the existing page-weight gate. The repository already uses React/React Router for server rendering; retain that build architecture. Add no client application framework, canvas library, or 3D bracket system.

Reuse PlayShell, HeroHeader, TabBar, PersonRef, MatchCard, and existing public DTO/formatting seams. Complete supporting API/projection work where data is missing. Preserve privacy, publication rules, stable identities, results, and entry/account workflows. Avoid unrelated architecture changes.

This source-grounded plan is not a fresh visual sign-off on the cited PDF. The agent must capture the implemented application. Historical competitor comparisons and screenshot measurements in the critique are design context, not newly verified measurements.

## 2. Execution matrix

| Done | Package | Work | Depends on | Findings |
| --- | --- | --- | --- | --- |
| [x] | P0 | Fixtures, baseline, and contract updates | — | C-PE-06; shared prerequisites |
| [x] | P1 | Shared tournament frame and breadcrumbs | P0 | C-PE-01, C-PE-21, C-PE-29, C-PE-30 |
| [x] | P2 | Player links, profiles, and player browsing | P1 | C-PE-19, C-PE-20 |
| [x] | P3 | Public match grammar, scores, courts, and references | P2 | C-PE-04, C-PE-23, C-PE-25, C-PE-27 |
| [x] | P4 | Scrollable bracket and round/path navigation | P3 | C-PE-24, C-PE-26, C-PE-28 |
| [x] | P5 | Season discovery and entry signals | P1 | C-PE-10, C-PE-11, C-PE-12, C-PE-13 |
| [x] | P6 | Overview, Draws index, and regulations | P1, P3 | C-PE-14–18, C-PE-31 |
| [x] | P7 | Forms, controls, local time, and final copy | P1–P6 | C-PE-02, C-PE-03, C-PE-05, C-PE-22 |
| [x] | P8 | Integration, page weight, and surface book | P0–P7 | All 28 findings |

The critique defines **28 findings**: C-PE-01–06 and C-PE-10–31. It contains no C-PE-07, C-PE-08, or C-PE-09.

## P0 — fixtures and contracts

**Start in:** existing fixture/seed tooling, `apps/entrant/tests/helpers/`, `docs/reference/contracts/match-card.md`, `docs/reference/contracts/state-and-formatting.md`, and public surface-book capture definitions.

- [x] Record the checkout SHA, fixture mode, viewport, event timezone, and route for the baseline. **Baseline: branch `feat/surface-book-remediation` at `2f2708d8`, `FIXTURE_MODE=normal`, viewports desktop 1440×900 / mobile 390×844 CSS px at 2×, event timezones `Asia/Taipei` (T029) and `Asia/Seoul` (T030), entrant baseline route `/e/` (console `/`).** All five are now written into the capture manifest and printed on the review-book cover: `tools/surface-capture.mjs` `captureContext` gains `checkoutSha` (from `git rev-parse HEAD`, overridable with `CHECKOUT_SHA`) and `baselineRoute`, beside the existing `fixtureMode`, `eventTimeZone` and `viewports`; documented in `docs/how-to/run-the-shared-fixture.md` ("What a capture records about its baseline").
- [x] Add a real, published player fixture whose existing profile URL returns a useful page, not the S46 missing-player 404. **Root cause was the fixture, not the renderer:** the public person directory is built from *confirmed entries* (`entries_site.py::_public_identities`) and the seed created none, so `entrants_published` was `false` and every `/e/{slug}/players/{key}` 404'd. `simulator/tournament_sim/seed.py` now registers the whole draw through `POST /tournaments/{id}/entries/import`, confirms every entry, publishes entrants, and uses the resulting `entry_players.id` as the bracket roster id (`entry-{uuid}`). Verified against a live fixture: **252 of 252 public players resolve on both tournaments** and `/e/2026-korea-masters-t030/players/{playerKey}` returns a profile with events, seed, club, draw path and matches.
- [x] Include verified linked identity across multiple tournaments so profile history can be exercised. Keep a separate missing/private-player fixture for legitimate 404 testing. Two **personal** entrant accounts (`featured.singles@…`, `featured.doubles@…`) each own exactly one named person, registered in **both** T029 and T030 — `Alex Lanier` (MS) and `Amallia Cahaya Pratiwi` (WD); the other four accounts are club managers that deliberately own many differently-named players, so `account_id` alone is demonstrably not a person key. `fixture.json` now carries `playerKey`/`playerName`, `linkedPlayerKey` (same human, other workspace), `withheldPlayerKey` (registered then withdrawn → "Player not published" + a real 404) and `missingPlayerKey` (well-formed UUID, no row). `tests/e2e/prepare-console-fixture.py` asserts the 200 and both 404s before writing the file.
- [x] Add completed and live matches with published scores, approved court assignments, seeds, and stable human match references. 103 recorded Taipei results with paired game scores; **six live matches, one per court, on all six courts**; **80 seeded participants** (top 8 in each of the 10 draws). Seeds needed a product fix as well as fixture data: `apps/api/src/bracket/io/import_matches.py` silently dropped `ParticipantIn.seed` **and** `entryPlayerId` on every pre-paired import, so neither reached `bracket_participants` or the public `TeamDTO.seed`. Match references are the one item **not** delivered — see the last box.
- [x] Include resolved later rounds, singles, doubles, long names, retirement, walkover, unpublished scores, and truly unresolved future sides. Taipei's five disciplines now sit at deliberately different depths (`_DEMO_LIVE_PROGRESS`): MS in its opening round, WS with R16 half played, MD in the quarter-finals, **WD played out to its final**, XD with a fully resolved and entirely unplayed R16. One walkover (MS R32, no score at all) and one retirement (WS R32, `21–15, 11–7`) via `_DEMO_EXCEPTIONAL_RESULTS`. Unpublished scores = Korea (`resultsPublished: false`). Unresolved future sides already existed and are preserved (QF/SF/F carry `unresolved.kind: winner_of` with no participant key). Longest doubles pair in the fixture: `Muhammad Reza Pahlevi Isfahani / Kang Khai Xing` (47 chars).
- [x] Replace generated tournament descriptions and semicolon draw inventories with concise, believable organizer prose. Do not rewrite real organizer content as fixture cleanup. `_DEMO_ORGANIZER_PROSE` / `_DEMO_REGULATIONS_PROSE` replace "Fictional, badminton-plausible demo of the Super 300 …", the `Draw listing: 32-entry men's singles draw; 32-entry women's singles draw; …` sentence and the `host; date_range; draw_format` venue address. **This is fixture data only** — the NOTES file's provenance is still parsed, still reconciled and still written into the manifest's `source` block; it simply stopped being reader-facing copy. Also fixed: entry events published `mens_doubles_final` as a discipline name on the public profile page; they now publish `Men's doubles`.
- [x] Seed realistic local entry windows and play dates. Separate deliberately invalid/conflicting fixtures from the normal visual book. Windows are written in **venue-local time with the venue's real UTC offset** (`_demo_entry_window`): Taipei closed 14 Jul, Korea's four main events 21 Jul, and Korea's **mixed doubles open until 1 Aug** — one genuinely open window beside four closed ones at the frozen clock. Play dates needed a projection fix: `ScheduleMatchDTO.scheduledDate` published the tournament's *start date* for every match, so a six-day event grouped 155 matches under one day heading; `entries_site.py::_slot_date` + `shared/schedule_slots.py::slot_day_offset` now derive the calendar day from the plan slot (days facet is now 29 Jul – 2 Aug). Invalid/conflicting states remain `FIXTURE_MODE=failure` only, unchanged.
- [x] Update contracts for the unified frame, public empty-side treatment, paired scorelines, native bracket scrolling, and venue-local time presentation. `docs/reference/contracts/state-and-formatting.md`: new public-P0 amendment banner, §5.1 rule 6 (public paired scorelines), §6.1/§6.2 (the public empty side, specified), §7.1 (venue-local presentation, superseding `clock_with_zone` for public tournament pages), §7.4 and §6.4 deltas, and a **new §11 "The public tournament frame"** (hero/tabs/breadcrumbs composed once, with its own missing-data, authority and delta tables). `docs/reference/contracts/match-card.md`: public-P0 amendment banner plus in-place §2.2, §4.2 and §4.3 rewrites (native `overflow:auto` bracket region, sticky round headers, content-derived node height, withdrawn height ceilings). `npm run docs:build` passes.
- [x] Coordinate shared identity/score conventions with operator-visual-fixes.md. Public nodes and operator match lists must refer to the same match. Written into both contracts (state-and-formatting §6.1 "One reference, both tiers"; match-card §2.2/§4.2/§4.3): one authority, one string, `MS R32·11` / `MS R16·2` — the console's `matchIdentity.ts` grammar, of which the plan's `MS R32-11` is the same reference. **The wire cannot carry it yet** and that is recorded as a delta row, not silently skipped: the public DTOs publish `MatchNodeDTO.position`, `ScheduleMatchDTO.matchKey`/`roundLabel` and a locally spelled `"SF 1"`, and none of the identity coordinates the console formats from. Publishing them is P3 work; P0 fixes the rule and forbids the local renumbering that made `Match 1` look acceptable.

**Done when:** profiles, actual scores, known courts, and populated later rounds can be captured without relying on fabricated display values.

## P1 — one tournament frame and breadcrumb system

**Start in:** `apps/entrant/app/components/PlayShell.tsx`, `HeroHeader.tsx`, `TabBar.tsx`, and `app/routes/tournament.tsx`, `schedule.tsx`, `draw.tsx`, `regulations.tsx`, `player.tsx`.

- [x] Compose one shared tournament frame containing the tournament hero and the same tab bar on Overview, Schedule, Draws, Players, draw detail, and Regulations. New `app/components/TournamentFrame.tsx` (breadcrumbs + `HeroHeader` + `TabBar`) over new `app/lib/tournamentFrame.ts` (`tournamentFrameModel`, `frameTabs`, `sectionHref`), rendered by all seven routes; `tests/tournamentFrame.render.test.ts` renders every route and asserts one `h1#tournament-title`, the same five-tab bar and one venue-time line on each (28 assertions, green).
- [x] Keep tournament identity, relevant live CTA, and navigation consistent at every tournament depth. Use the shared frame for tournament-scoped player detail too. `phaseAction`/`ctaState` moved out of `tournament.tsx` into `tournamentFrameModel`, so the draw detail, the player page and the regulations reader now carry the same hero, organizer, date/venue line, status line and lifecycle CTA the Overview does. `player.tsx`'s loader returns the whole `EntryPageDTO` for that (it previously kept four hand-copied fields).
- [x] Centralize breadcrumb rendering in the shell/frame above the hero: **Tournaments › Korea Masters › Draws › Men's Singles**. New `app/components/Breadcrumbs.tsx`, rendered by the frame inside the hero band above the title; a route supplies only the tail it alone knows (`trail`). The draw detail's real trail is `Tournaments › Spring Open › Draws › Men's Singles`, pinned per route in `tournamentFrame.render.test.ts`.
- [x] Make ancestor segments native links; mark the current segment as the current page. Generate labels from actual tournament/event data. Ancestors are `<a href>`, the last segment is a `<span aria-current="page">` (never a link back to itself); labels come from `page.tournament.name`, `eventDisciplineLabel(draw.discipline)` and `personRefModel(...).text` — §11.2's rule is implemented as an omission (a tournament with no name drops the segment rather than printing the slug).
- [x] Remove floating `← Tournament`, `← Tournament · Draws`, and per-page text-arrow back links. Deleted from `draw.tsx`, `player.tsx` and `regulations.tsx`; a per-route assertion forbids `← Tournament`, `← Spring Open` and `Tournament page` anywhere in the document. The two remaining arrows on the tier are pagination (`← Previous`, `← Previous round`) and the entry form's own back link, which is out of §11's route scope.
- [x] Remove Regulations' separate left navigation and competing facts/header frame. Keep document actions within the shared page hierarchy. The `md:grid-cols-[14rem_...]` aside is gone with its Overview/Draws/Players link list (which restated the tab bar) and so is the eyebrow + `h1` + tournament-date/venue/organizer `dl` that competed with the hero. Print/Download stay, beside the document's `h2`; the document's own outline survives as an inline `Document sections` nav above the text. `regulations.render.test.ts` now asserts each of Overview/Draws/Players appears exactly ONCE on the page.
- [x] Remove Schedule's separate hero implementation and repeated timezone, entries-status, and freshness additions. Feed the same hero data into every tournament route. `schedule.tsx` lost its own `HeroHeader`/`TabBar` composition, its `chipState`/`tournamentPhase`/`visibleTabs` calls and its `freshness` prop; `HeroHeader`'s `freshness` prop is deleted outright so no route can reintroduce one. The page said `Asia/Seoul` three times (hero suffix, hero fallback, subtitle) and now says it zero times: `schedule.test.ts` asserts `not.toContain("Asia/Seoul")` plus the absence of any `GMT±n`/`KST` spelling.
- [x] Highlight the correct parent tab for nested pages. Keep Regulations reachable through Documents/breadcrumbs without inventing a conflicting second tab system. `TabBar` gained a fifth entry, `Documents` → `/e/{slug}/regulations`, present only when the organizer published regulations (§11.2); a draw detail highlights Draws, a player page Players, the reader Documents. The per-route test asserts exactly one `aria-current` tab, exactly one `aria-label="Tournament sections"` nav and exactly one breadcrumb per document.
- [x] Keep any useful schedule-updated information as one muted line below the list, formatted without a raw timezone identifier or offset. One `text-xs text-muted-foreground` line after the pagination: `Updated Sep 12, 2026, 7:35 PM`. `formatScheduleUpdated` still CONVERTS into the venue timezone (contract §7.1: removing the suffix is not the fix) and dropped only `timeZoneName: 'short'` — the unit test pins both halves, that 10:35 UTC reads 7:35 PM in Seoul and that no zone spelling survives.
- [x] Keep the hero compact enough on narrow screens that primary content remains easy to reach. The band steps down at mobile and grows at `md:`: `pt-8 md:pt-10` → `pt-3 md:pt-5`, the content row `gap-y-6 pb-6` → `gap-y-4 pb-4 md:pb-6`, the tab row `pb-4` → `pb-3 md:pb-4`, and the title `text-[1.875rem]` → `text-2xl md:text-[1.875rem]`. No `overflow-hidden`, `nowrap` or clamp was used to buy the space (`noTruncation.test.ts` is green).

**Done when:** a reader can navigate several levels deep without losing tournament identity, tabs, or a clear route back.

## P2 — PersonRef, profiles, and Players

**Start in:** `app/components/PersonRef.tsx`, `PersonGroup.tsx`, `EntrantsList.tsx`, `PlayersList.tsx`, `public/assets/person-ref.js`, `entrants-filter.js`, `app/routes/player.tsx`, `app/lib/player.types.ts`, and `apps/api/src/entries/entries_site.py`.

### Links and profile v1

- [x] Use PersonRef for every resolved published player's name in Players, bracket nodes, schedule cards, results, partners, and profile/history lists. Verified on the live fixture, not by inspection: rendered `/e/2026-taipei-open-t029/draws/MS` carries **94 `person-ref` nodes, 74 of them anchors**, and the 20 spans are *every one* a structural placeholder (`Winner of R32 11` … `Winner of SF 2`) — no human name renders as unlinked text; the schedule's 54/16 split is the same shape (38 `Winner of …` spans). Bracket nodes and schedule cards reach `PersonRef` through `PersonGroup`/`MatchCard`, the Players directory through `EntrantsList`, partners and draw paths through `player.tsx`, and the new history rows through `personHref` — the same resolver, so no second anchor renderer exists to drift.
- [x] Trace missing links through the person projection and fixture identities. The component already creates links when a valid identity is supplied; adding another anchor renderer is not the fix. Traced with a local SQLite + uvicorn fixture (P0's route, no Docker): a walk of every public projection found **252/252 directory people resolved with an id on both workspaces**, one deliberate `Player not published` per draw (the withheld fixture person) and nothing else dead. The one genuine wire defect found was a *latent* one in `_person_ref`: the draw passed a bracket event's raw id (`T027-MS`) while an entry-backed person's `visible_events` may hold only the desk's spelling (`MS`), so a namespaced import turned every eligible name into `Player not published`. `_person_ref`/`_participant_people` now take `event_alias` exactly as `_event_public_for_person`/`_event_public_club` already did; regression pinned by `test_a_namespaced_bracket_event_still_links_its_entry_backed_people`.
- [x] Use one shared link-target resolver and stable person identity. Never derive a player key or cross-tournament match from a name string. `personHref(slug, identity)` in `public/assets/person-ref.js` stays the single resolver and now serves cross-tournament targets too (`personHref(row.slug, { id: row.playerKey, … })` in `player.tsx`) — `personRef.test.ts` pins that it returns the same URL `personRefModel` does and `null` for any identity with no id. The cross-tournament JOIN is server-side and canonical: `_canonical_person_key(account_id, folded name)` — the verified entrant account, narrowed by the stored name because one account legitimately owns several people. `test_history_is_never_a_name_match_across_accounts` pins that two strangers sharing a name stay two profiles; `test_history_links_the_same_human_across_workspaces_by_verified_account` pins that one account's *other* player is not merged in.
- [x] Make every eligible name lead to a real profile. Keep unknown/withheld identities non-linking and preserve privacy gates. Every one of the 252 published people on each workspace answers 200 at `/e/{slug}/players/{identity.id}` (walked live); `withheldPlayerKey` still answers 404 and still renders as `Player not published`, `missingPlayerKey` still 404s. The directory's club now comes from the SAME gated person directory as the name (`identities.clubs`) instead of a second, wider desk query — one authority for both public fields on a row (`test_the_directory_reads_club_from_the_same_gate_as_the_name`).
- [x] Complete profile v1 as **identity + tournament history**, with links into that player's public event/match records. `player.tsx` gains a `Tournament history` section below the match groups: one row per public workspace, newest first, each linking to *that* workspace's page for the same human (`/e/winter-classic/players/{its own key}`) with the venue-local date and that workspace's event codes. The linked page is itself the person's event/match record — events, seed, partner, draw path, Coming up and Played. Pinned by three cases in `player.render.test.ts`.
- [x] Extend the public profile projection where needed: the current player DTO is tournament-scoped and explicitly omits cross-tournament records. Supply history through verified canonical identity links, not client-side name matching. `PlayerPageDTO.history: List[PlayerHistoryEntryDTO]` (`slug`, `tournamentName`, `date`, `endDate`, `playerKey`, `current`, `eventCodes`, `drawsPublished`, `resultsPublished`), built by `_person_history` in one query joined on the entrant account — no client-side matching of any kind, and the entrant hand-mirror is held to the generated OpenAPI by `dtoParity.test.ts` (`make generate-api` run; `dto.generated.ts` +41 lines). The allow-list guard `test_public_person_contract.py` carries the new shape, which also puts it under the existing `accountId`/`email`/`phone` disjointness assertion.
- [x] Include only published, permitted history. Retain useful current-tournament information for people who have no linked history yet. Another workspace appears only when it is `is_open`, `audience == "public"` (an *unlisted* page is reachable but deliberately undiscoverable, so a public profile may not publish it), `entrants_published`, with a **confirmed, non-opted-out** entry and a non-erased person — six parametrized cases in `test_history_omits_a_workspace_that_has_not_published_its_own_list` / `…_the_person_is_not_public_in`. The current workspace is always present and marked `current`, so a person with no linked history gets a history of one rather than an empty section (`test_history_always_carries_the_current_tournament_marked_current`); on the live fixture 223 of 252 people have two rows and 29 have one.
- [x] Preserve existing tournament-person URLs as valid entry points even if the canonical account/profile destination changes. `/e/{slug}/players/{entry_players.id}` is unchanged — no route added, no key reshaped, no redirect introduced; the whole existing `test_entries_site_api.py` player-page suite still passes untouched apart from the one key-set assertion that gained `history`. Every history row is expressed in exactly that URL shape, so the canonical identity is a JOIN, never a new address.
- [x] Verify the journey Players → profile → past tournament → draw/schedule, including browser back and no-JavaScript navigation. Walked against the live fixture through the SSR server with plain HTTP GETs and no browser: `/e/2026-korea-masters-t030?tab=players` → Alex Lanier's profile → the `Tournament history` link to `/e/2026-taipei-open-t029/players/{linked key}` → that page's own history linking back to Korea, plus its draw path and matches. Every step is a native `<a href>` in the served HTML (no `<Scripts/>` on this tier at all), so back is the browser's own history entry and nothing depends on JavaScript.

### Browsing

- [x] Preserve the strong Players layout: count, A–Z index, three desktop columns, and muted event codes. All four are untouched by the toolbar change: one `[data-search-count]`, the letter index over only the letters present, `sm:grid-cols-2 lg:grid-cols-3`, and `text-xs text-muted-foreground` event codes. The nine pre-existing `EntrantsList` assertions in `components.test.ts` still pass unmodified.
- [x] Make search and the A–Z index one sticky toolbar. Set offsets so it does not collide with other sticky navigation. Count, search mount and A–Z index now live in one `[data-directory-toolbar]` element, `sticky top-0 z-20` over an opaque `bg-surface-base` band bled to the gutters (`-mx-4 px-4`). `top-0` is a *stated* claim, not a default: nothing above it on this tier is sticky (the shell header and the whole tournament frame scroll away), and `TOOLBAR_OFFSET` is the one place the offset lives, paired with `JUMP_CLEARANCE` so both halves move together if that ever changes. Pinned by `composes the count, the search mount and the A-Z index into ONE sticky toolbar (P2)`.
- [x] Keep letter jumps visible below the sticky toolbar and keyboard accessible. Each letter section carries `scroll-mt-28` (the toolbar's own height, from `JUMP_CLEARANCE`) so a jump lands *below* the bar rather than underneath it, and `tabIndex={-1}` so the fragment moves FOCUS and not just the viewport — without it the next Tab returns to the toolbar instead of continuing into the chosen letter. Each index link carries a real focus ring and an unabbreviated name (`aria-label="Jump to P"`), because a one-glyph link is otherwise unreadable to anyone who arrives on it by keyboard. The script also hides a jump whose letter the current query emptied, so the index never offers a dead anchor.
- [x] Make **Name or club** search accurate. Match authorized club data and show club context where relevant, especially for club-only matches. Accuracy was the defect: the box promised "Name or club" but compared raw lowercased strings, so `kjaer` never found **Rasmus Kjær**, `arin` never found **Neslihan Arın** and `nguyen` never found **Nguyễn Thùy Linh** — all three are in the canonical fixture. `searchKey` (NFKD + combining-mark strip + an explicit table for ø/æ/ð/þ/ß/ł/đ/ı/œ, which Unicode will not decompose) now normalises BOTH the SSR `data-name`/`data-club` and the typed query, from one exported function so the two halves cannot diverge. Club data comes from the gated directory (previous box). `matchField` reports *which* field matched and `apply()` promotes the club line out of the muted register on a club-only hit (`data-club-match`), so a club search no longer returns a screen of names with no visible reason. Six new script assertions.
- [x] Retain no-results handling and access to the complete list after clearing search. The SSR-shipped `[data-no-matches]` line and the clear-back-to-everything behaviour are unchanged and still pinned by the two original `apply` tests (`reveals the no-matches line only when the whole list is gone`, `matches on club too, and clears back to everything` — 3 rows back, count reads `3 players`); clearing also restores every letter group and every A–Z jump (`hides an A-Z jump whose letter the query emptied, and restores it`). With JavaScript off there is still no search box and therefore no dead control — the full list is the document.

**Done when:** names are consistently linked, the profile fixture works, public history is correct, and search/index remain usable throughout a long Players page.

## P3 — public match grammar and complete match context

**Start in:** `app/components/MatchCard.tsx`, `PersonGroup.tsx`, `app/routes/draw.tsx`, `schedule.tsx`, `player.tsx`, `app/lib/draws.types.ts`, `schedule.types.ts`, `side.ts`, and public match projections.

- [x] Reuse the shared MatchCard semantics for public schedule cards and bracket nodes, with separate surface-appropriate layouts. All four public match surfaces still reach exactly one component. `MatchCard`'s three variants now differ where the surface differs and nowhere else: the **card** keeps a column per side per game (§4.2 "each side's games sit on that side's own row"), the **bracket node** replaces them with one right-aligned paired lane because a 288px node cannot hold four score columns beside two doubles pairs (§4.3), and the **compact list** row gained the footer it never had, so a draw's List/Round views finally state time and court at all. The semantics — sides, seeds, the winner mark, the ledger's no-padding rule, the state vocabulary, the accessible `versus`/`and` phrase — are shared code, not shared conventions.
- [x] Use the same human match reference as the operator: **MS R32-11**, **MS R16-2**. Remove repeated bare **Match 1, Match 2** labels. **Proved on the fixture, not asserted.** A throwaway probe ran the OPERATOR's own authority (`buildPlayUnitIdentities` → `formatMatchIdentity`, imported from `apps/console/src/`) over the live Taipei bracket DTO and compared its string with the `reference` the public projection published for the same node: **155 of 155 play units identical, 0 differing** (`MD R32·1`, `MS R32·11`, `WD SF2`, `XD F`…). `Match n` is deleted from the tier — `matchNumber` is gone from `MatchCardData`, and `draw.render.test.ts` now asserts its ABSENCE where it used to assert its presence.
- [x] Generate references through the existing identity authority/projection. Do not introduce tournament-wide numbering or recompute identity from rendered order. New `apps/api/src/shared/match_reference.py` is the backend twin of `matchIdentity.ts` — the same coordinates (event code, phase kind, round index, stage, segment id, 1-based sequence), the same `MAIN_SEGMENT` map, the same `·` separator, the same GF-reset and main-segment rules. It reads no row and no request; `_unit_locator` feeds it the stage it already derived with `_short_round` off the same (total, index) pair the console derives it from, so there is no second stage ladder to drift. Numbering is per (event, segment, round) exactly as the console's is — never tournament-wide, never the rendered row index. `tests/backend/unit/test_match_reference.py` transcribes the console's own `matchIdentity.test.ts` case table verbatim (21 cases): if the two formatters ever disagree, that file fails rather than the reader finding out.
- [x] In a draw with clear event context, use the compact line **R16-2 · 10:00 · Court 3**. In mixed-event schedules include the event code. One flag on one authority (`format_match_identity(..., include_event=False)`), one field on the wire (`shortReference`), and the route picks by what its context already states. Live Taipei render: the MS draw's List view reads `R32·1 · Wednesday, July 29 · 11:00 · Court 5` and its nodes are headed `R32·1` / `QF3` / `F`; the whole-day schedule reads `MS R32·13 · 13:00 · Court 6` and `XD R16·1 · 13:00 · Court 4`. Pinned by `draw.render.test.ts` ("uses the event-code-dropped reference in this single-event view", which also asserts the draw page never says `MS SF1`) and `schedule.test.ts` ("names the match with the SHARED reference on one compact line").
- [x] Show the approved court on the match card, including in By time mode. Trace missing court data through the projection, not just the renderer. The court was already on the wire and already on the card in every grouping mode; the trace is what this box actually needed, and it found the projection **correct**. `_schedule_runtime_snapshot` publishes Operations' materialized `matches.court_id`, `_merge_live_bracket_courts` adds currently-running bracket claims, and a plan-only future placement is deliberately not an approved court. On the P0 fixture that produces 8 live matches over 6 courts — **courts 1 and 2 each carry two live claims**, so `shared.court_occupancy` marks them disputed and the four affected matches publish no court at all (ruling C2). That is the next box's rule working on real data, not missing court data.
- [x] Do not publish a disputed or withheld court, invent a court, or call a courtless record “On court”. Retain truthful non-assertive presentation for that edge case. The renderer half was the gap: a live record with no court still said **On court**, which is a claim about a court. `MatchCard` now suppresses the state word for exactly that case — the "Live now" band still says the group is live (§3.3: one live band per group), and the card asserts nothing it cannot support. `"Scheduled"` went with it: §4.2 forbids a Scheduled chip over a card with no approved time, and the compact line's `10:00` / `Time to be confirmed` is the more precise answer anyway. Pinned by `schedule.test.ts` ("never calls a courtless live record 'On court'"), which carries a positive control — with a court approved the same card says both.
- [x] Remove repeated per-card dates inside an already day-grouped schedule. Preserve date context for multi-day or ungrouped cards. `showDate` was `!filters.day`, which repeated the date on every card of an unfiltered list even when every match fell on one day. It is now `!filters.day && visibleDays.size > 1` — computed from the items actually rendered, so the date disappears the moment the list is effectively one day and **returns the moment it genuinely spans days**, which a day-filter-only rule would silently break. The player page had the sharper version of the same defect: it handed the wire's bare `2026-07-29` straight to the card (D12, raw ISO in prose) while `draw.tsx` humanised it — `playerMatchCard` now routes both through the one entrant time authority.
- [x] Stack the two sides clearly; stack doubles partners within each side. Make doubles nodes taller than singles nodes. Partners were already one per line (`PersonGroup`, V3-PE14.1) and each side is its own grid row, so a doubles node is structurally two lines taller than a singles one. What was missing was the §4.3 size floor: bracket-node names rendered at `text-xs` (12px), which the contract **rejects as a final target**. They are `text-sm` (14px) now, and the node's `min-h-[58px]` stays a floor rather than a height, so the node grows for the content instead of clipping it. Node geometry is content-derived; no fixed height was introduced.
- [x] Show available seed values and real paired game scores, e.g. **21–16, 22–20**, right-aligned in bracket nodes and clearly associated with sides in schedule cards. Seeds moved out of `PersonGroup`'s block-below-the-last-partner position into the side row's own trailing right-aligned cell, so `[3]` sits beside the side it belongs to on both variants. Scores split by surface as §4.2/§4.3 require: the card keeps a cell per side per game; the node renders one right-aligned `pairedScoreLine` on its trailing edge (`21–18, 21–12` on live Taipei nodes). Pinned by `draw.render.test.ts` ("renders the paired score once, right-aligned on the node trailing edge", which also asserts no `place-items-center` score cell survives in a node).
- [x] Keep score orientation in first-listed-side order and bold only the winning side from the authoritative outcome. Follow the approved operator convention of no per-game bolding. `pairedScoreLine` takes `game[0]` then `game[1]` — the wire's own side order, which is the order the two side rows render in. Per-game emphasis is deleted: the score cell's `font-semibold` and its winner-conditional ink are both gone, so no game score is ever bolded or coloured (§5.1 rule 6). The winner is the side's NAME, and its source changed: `scheduleToMatch` used to count games won (`gamesWon`, deleted) and call the higher total the winner — the exact inference retirement and walkover break. `ScheduleMatchDTO.winnerSide` now carries the recorded outcome, pinned by `schedule.test.ts` ("takes the winner from the authoritative outcome, never from the ledger") with a retirement whose ledger and winner disagree.
- [x] Show the current game when live score data is published. Do not show a scores-update promise unless the page actually supports the claimed updates. The ledger renders exactly the games the wire carries, in-progress ones included and never padded (§3.4) — a live card with two recorded games shows two, in the same weight as a completed one. The promise is deleted: "Scores update as the desk records them" sat above the Live band on a tier that ships **no client framework and no polling**, so the document a spectator was reading would never change under it. The freshness line below the list (`Updated Sep 12, 2026, 7:35 PM`, venue-local, P1) is the honest version of the same idea. Pinned by "makes no promise to update scores it cannot update".
- [x] Preserve retirement, walkover, and no-score cases; do not invent 0–0 scores or derive a match winner from the ledger. Verified on the fixture's real exceptional results, not on a constructed case. The Taipei walkover renders `Walkover · MS · Round of 32 · Winner: Li Shifeng` with **no ledger at all** — no cell, no reserved width, no 0–0; the retirement renders `Retired · WS · Round of 32` keeping the partial `21–15, 11–7` that was actually played. Neither winner comes from the ledger: the bracket path reads `result.winner_side`, and the schedule path now reads `winnerSide` off the wire (previous box). `matchCard.contract.render.test.ts` additionally asserts a walkover node contains no en-dash at all.
- [x] Replace unresolved “Winner of …” player text with empty participant slots and a muted feeder line. Keep feeder relationships in connector/data semantics, without exposing machine IDs or the phrase “Winner of”. `feederLabel` in `app/lib/side.ts` replaces both `Winner of ${ref}` and `Loser of ${ref}` with `from {reference}`, and `PersonGroup` renders it as an EMPTY participant slot (`data-feeder-slot`) in the muted register rather than a name-shaped placeholder — the slot keeps a name's own line height, so nothing jumps as results land. The relationship stays in `feederNodeKey`/`feederTake` and the connector column, never in visible prose. Live MS draw: **20 feeder slots, 40 `from R32·n` lines, zero occurrences of "Winner of", zero node keys in prose**. The reference in that line is the SHARED one, so `from R32·11` resolves to the node headed `R32·11` by reading rather than by counting rows (V3-PE10.1).
- [x] Preserve known members of incomplete doubles sides. Keep byes and withheld people distinct from an unknown feeder; never invent a person to fill space. `pending_member` is untouched and still prints the known player AND "partner to be confirmed" on its own line. The three absences now read as three different things, which is the distinction the old treatment lost: a **bye** and a **withheld person** stay in the NAME register ("Bye", "Player not published") because both are settled facts about the slot, while only an unreached feeder is muted. Pinned by a new case in `matchCard.contract.render.test.ts` ("a bye and a withheld person stay distinct from an unknown feeder") asserting neither carries `data-feeder-slot`.
- [x] Use a small leading state cue for exceptional match states; avoid redundant status furniture. `isExceptionalState` (walkover / retired / cancelled / delayed) drives a small LEADING cue before the event·round heading — and suppresses the trailing state word, so a card never says the same thing twice in two positions. The routine words the surrounding context already states are gone: "Scheduled" (the time says it), and "On court" on a courtless record (nothing says it, truthfully). "Completed" survives because a completed match with withheld scores has nothing else to say it. Live render: the walkover and retirement cards each carry exactly one state word, at the head.

**Done when:** desk, bracket, schedule, and player page agree on match identity, sides, court, score, and outcome; future slots no longer look like generated player content.

## P4 — bracket scrolling, spacing, rounds, and player paths

**Start in:** `app/routes/draw.tsx`, `app/app.css`, `public/assets/bracket-path.js`, and `tests/e2e/tests/11-public-bracket-geometry.spec.ts`.

- [x] Put the bracket in its own **overflow: auto** container, with height bounded to the available viewport. Keep page-level horizontal overflow off. — `draw.tsx` `[data-bracket-scroll]` + `.bracket-scroll { max-height: min(78vh, 46rem) }`; geometry spec asserts `overflowX: auto`, region height ≤ viewport and `documentElement.scrollWidth <= clientWidth` before and after scrolling to the Finals (the `relative` on the region is what stops the tree's `sr-only` spans escaping the clip and giving the page 33px of sideways scroll).
- [x] Give the scroll region an accessible name and keyboard access; allow normal vertical and horizontal scrolling without trapping the page. — `role="region" tabIndex={0} aria-label="Men's Singles bracket"`, no `overscroll-behavior` (chaining left on, so an edge hands the scroll back); pinned by `draw.render.test.ts` and the geometry spec.
- [x] Keep round headers sticky at the top of the bracket container. — `.bracket-round-header` (sticky, opaque `--surface-raised`), the region carries no top padding so it sticks flush; geometry spec scrolls the region 200px and asserts `position: sticky` with < 2px offset from the region's top.
- [x] Use **R32 · R16 · QF · SF · F** round controls that jump the container to actual columns. Adapt labels to the draw format. — new `roundShortLabel()` (display twin of the backend `_short_round`) drives `RoundControls`; a 32 draw renders R32·R16·QF·SF·F, a 4 draw SF·F, a round robin R1·R2 (geometry spec + `draw.render.test.ts`).
- [x] Use native anchors as the no-JavaScript round-jump mechanism; enhance scrolling with a small page-scoped script if needed. — `<a href="#draw-round-semifinals">` into real column ids; the geometry spec clicks one with NO script loaded and asserts the region's `scrollLeft` moved and the page did not. `bracket-path.js` upgrades the same anchors to a smooth in-region scroll with no `#hash`.
- [x] On narrow screens, use horizontal scrolling and snap-to-round behavior without shrinking names to fit every column. — `snap-x snap-mandatory md:snap-none` + `snap-start` per round column; geometry spec at 390px asserts snap type, a scrollable region, no page overflow and names still ≥ 14px.
- [x] Make **Round** navigation part of Bracket rather than a separate primary page mode. Preserve the useful List view. — `DrawViewLinks` is Bracket · List; the bracket is the default at every width and renders ONCE (the `md:hidden`/`hidden md:block` pair that shipped the draw twice is gone). `draw.render.test.ts` pins both.
- [x] Keep legacy `?view=round` links functional by selecting/positioning the requested round; remove duplicate previous/next headings and bare Match n labels. — the loader maps `round`/`path` to the bracket, `data-initial-round` positions it and the control gets `aria-current`; `RoundPager` ("← Previous round", "Round n of m") deleted, `Match n` already gone in P3.
- [x] Increase first-round separation toward the requested **8–12px** starting range and derive node height from actual content. Doubles must not be squeezed into a 44px ceiling. — `.bracket-slot { padding-block: 5px }` (10px between first-round neighbours, measured 8–12 in the browser), `min-h-[46px]` on the slot and `min-h-[58px] w-72` on the node both deleted; geometry spec asserts a doubles node is strictly taller than a singles one with all four names whole.
- [x] Recalculate connector geometry for varied node heights; maintain clear feeder grouping and unclipped Finals access. — the brace column reserves the same `h-6` header strip as the round column, and equal `flex-1` slots keep braces on their destination's centre whatever the node height; geometry spec measures every brace against the node it feeds on a DOUBLES draw (max drift < 2px) and reaches the Final by scrolling.
- [x] Replace total draw-height ceilings and restrictive DOM-shape assertions with readable-content, non-overlap, scroll-reachability, and connector checks. Retain meaningful layout tests. — `11-public-bracket-geometry.spec.ts` rewritten: the `< 600px` / `< 1150px` canvas ceilings and `height >= 44 && children.length === 2` shape checks are gone; 9 tests now assert font floor + unclipped names, pairwise non-overlap of all 31 nodes, separation band, reachability, sticky headers, native anchors, brace alignment, 390px snap and the server-painted path. **Run in Chromium: 9 passed.**
- [x] Extend the existing bracket-path script for persistent player-path selection in the current bracket; highlight relevant nodes and connectors without reloading. — selection is persistent state in `mountBracketPath`, applied to nodes AND braces, with `replaceState` writing the shareable `?view=path&player=` URL; hover demoted to a preview that only runs while nothing is selected (`bracket-path.script.test.ts`).
- [x] Support click/touch and keyboard path selection, with a clear reset action. Do not rely only on hover. — pointer/touch/Enter all arrive as one `click` handler; reset is the toolbar's "Clear path" button, the Escape key, and (with no script at all) the server-rendered "Clear path" link.
- [x] Resolve the name-link/path interaction explicitly: normal PersonRef activation opens the profile; in an explicit **Highlight path** mode a plain name activation selects the path and exposes a **View profile** link. Preserve modified-click/new-tab behavior and the native profile href. — the toggle is built by the script into the SSR `[data-bracket-toolbar]` mount (no dead control without JS); tests assert a plain click is untouched with the mode off, prevented with it on, and NEVER taken for meta/ctrl/shift/alt clicks, with the profile href unchanged and re-exposed as "View profile".
- [x] Keep `?view=path` URLs as functional no-JavaScript fallbacks with the selected player preserved. — `?view=path&player={id}` renders the bracket with `has-person-path`/`is-person-path` painted SERVER-side on nodes and braces, the player named and a reset link; asserted in `draw.render.test.ts` and in the browser (script never loaded). A name search that matches exactly ONE person also offers `Show {name}'s path` → that same URL, which is the scriptless way to reach a pinned path and the only place a name is allowed to become an identity — the reader picks it, the page never assumes it.
- [x] Keep scroll-and-round navigation sufficient on its own. Do not add 3D or a canvas dependency; pinch/wheel zoom is not required by this plan. — no canvas, no transform, no new dependency; the tier's only addition is the existing page-scoped ES module, and the draw route is outside the measured page-weight set.

**Done when:** all rounds are reachable, names and doubles are readable, headers stay in view, path highlighting works in place, and native fallbacks still navigate.

## P5 — one season calendar and clear entry actions

**Start in:** `app/routes/discovery.tsx`, `components/SeasonCalendar.tsx`, `SeasonControls.tsx`, `SeasonStatusCell.tsx`, `NowStrip.tsx`, and the season-list API/filter helpers.

- [x] Replace visible lifecycle tabs/facets with **one month-grouped season list** and **year selector + search** in the toolbar. `SeasonControls` lost the three-segment `SegmentedNav` ("Live & upcoming" / "Entries open · N" / "Completed · N") **and** the whole `<details>` date popover (presets, from/to, year number field, "Filters · N" badge, the active-filter chip row). What is there now is a `<nav aria-label="Season">` of year links plus "All seasons", and one single-field search form. `SeasonCalendar` renders ONE shape — `seasonModel`'s month sections — instead of branching on a `view` prop; the `view`/`preset`/`from`/`to`/`page` vocabulary is gone from `Filters`, `parseFilters` and `filtersToParams`. `discovery.render.test.ts` asserts none of `Live &amp; upcoming`, `Entries open ·`, `Completed ·`, `aria-label="Calendar view"`, `<details>` or `data-active-filter-row` survives.
- [x] Retain the live strip at the top when there is a real live tournament. `NowStrip` is unchanged and still renders exactly when the SERVER picked one (`season.now`); the old `filters.view !== 'season' && 'open'` gate that hid it on two of three segments is gone with the segments. It now steps aside only for an active search, which is a deliberate question about something else. The §7-trap-1 test still passes: an `in_progress` row dated today conjures no band.
- [x] Show upcoming events ascending in full visual weight, then **Earlier this season**, then past events descending. `seasonModel` splits on `isPast` (publication first, then the calendar day — never the other way round, because the date is the field a director may not have kept current) and hands over `upcoming` ascending and `past` descending, both month-grouped. The `<h2 id="past">` reads "Earlier this season" (or "Earlier tournaments" under All seasons) and is absent entirely when the season has no past. Pinned by document order in `discovery.render.test.ts` ("reads top to bottom as time does").
- [x] Give past rows a muted but readable title, omit their venue line, and use a **Results** action only. One `past` flag on `CalendarRow`: the title drops `font-medium text-foreground` for `text-muted-foreground`, the meta array is `[]`, and `actionCell(row, true)` returns the results arm **whatever the row's stored status says** — an event that has happened cannot be entered, and offering the entry flow for it would be a dead end. Nothing published means `{ kind: 'text', label: 'Results not published' }`, an arm that carries no `href` field at all, so it cannot grow a dead link (§7 trap 3).
- [x] Include the selected season's past tournaments directly on the page. Remove "Looking for past results?" and the archive footer detour. Both the paragraph and the `/e/?view=completed#calendar` link it wrapped are deleted from `discovery.tsx`, along with the whole `Pagination`/`paginationPages`/`pageHref` block and `paginateRows` in `lib/phase.ts`. The past is on the page instead. `discovery.render.test.ts` asserts the absence of `Looking for past results?`, `View completed tournaments` and `Tournament pages`.
- [x] Remove the Entries open facet/count. On eligible upcoming rows show **Enter · closes 15 Aug**, linked to the actual entry flow; otherwise show **Entries closed** where entry status is relevant. The facet went with the segments (and `SeasonControls` no longer takes a `counts` prop at all). `actionCell` returns `{ kind: 'enter', href: '/e/{slug}/enter', closesAt, timeZone }` for `entries_open`; `SeasonStatusCell` renders `Enter · closes 30 Aug` against the fixture, `Enter` alone when the organizer set no deadline, and `Entries closed` for `entries_closed`. The href is the real entry route, not the tournament page.
- [x] Remove countdown suffixes and offset-heavy deadline prose. Closed past rows retain the quieter Results-only treatment. The `chip-open` arm — `Closes 30 Aug 2026, 13:00 GMT+1 · 5d` — is deleted, and `ActionCell`'s enter arm carries no `closesInDays` at all (a unit test asserts the serialized cell contains no day count). New `formatDayMonthInZone` gives `15 Aug` with no year, no time and no zone name. It **converts** into the event zone rather than trimming a suffix (contract §7.1) — pinned by a component test where 15:30 UTC on 31 July reads `1 Aug` in Seoul, which suffix-trimming would have called 31 Jul.
- [x] Keep the date block, name, and venue layout for upcoming events. Use one consistent right-side action slot and style. `DateBadge` + name + meta line is unchanged for upcoming rows (the R11 380px breakpoint test still holds the column shape). Every row, past or upcoming, now ends in the same `sm:min-w-[8rem] sm:shrink-0 sm:justify-end` slot holding one `ActionCell`, and `SeasonStatusCell`'s three link arms share one `LINK` class constant instead of the old chip's `text-xs font-medium` diverging from the links' `text-sm font-semibold`.
- [x] Remove duplicated year suffixes from display titles when the month/year grouping provides them. Preserve stored organizer names and handle display normalization explicitly. New `displayTitle(row)` in `lib/phase.ts` — a deliberately narrow, explicit rule: exactly one LEADING or TRAILING token equal to **this row's own** year, with the separator it sits against, and only when a name is left over. `2026 Taipei Open` under a `July 2026` header reads `Taipei Open`; `1992 Memorial Cup` in 2026 is untouched (that year is part of the name); `The 2026 Cup` is untouched (mid-name); a dateless row has no year to be redundant with and is returned verbatim. The stored name is never written to — this is display only.
- [x] Keep year and search state in shareable URLs. Preserve existing `?view=` deep-link behavior through appropriate section focus/selection or compatibility handling without restoring visible lifecycle tabs. `filtersToParams` writes `q` and `year` only, in one canonical order, and the loader's `canonicalTarget` redirects anything else off the URL: `?page=`, `?preset=`, `?from=`, `?to=`, `?view=open|all|season` land on `/e/`, and **`?view=completed` lands on `/e/#past`** — the section anchor `SeasonCalendar` puts on its `<h2 id="past">`, so a poster's archive link still reaches the results it promised without a lifecycle segment existing to select. Eight canonicalisation cases are pinned in `discovery.render.test.ts`, each with its destination asserted 200 so no redirect loops.
- [x] Handle unknown dates and missing metadata honestly; do not fabricate dates merely to place an event in a month. A dateless tournament belongs to no season, so it is listed in EVERY season under "Date to be confirmed" rather than hidden in all of them — active ones in the upcoming half, past ones in the past half — and the month grouper is never fed a row it cannot place. `displayTitle` leaves a dateless name alone, `DateBadge` still says TBC, and the `sr-only` long date is omitted rather than rendered empty (one `sr-only` element left on such a row, and it is the outline heading).
- [x] Make one season the content boundary and meet the page-weight gate. Avoid fetching or rendering every historical season at once. `resolveSeason` bounds the render to one year (this calendar year when it has rows, else the nearest future season, else the most recent past one); other seasons are a link, not markup. **Measured: `/e/` = 2.7 KB gzipped, 0 scripts, budget 4 KB — PASS.** That number is honest because `scripts/measure-page-weight.mjs` stopped measuring a one-row list: its season fixture is now 24 tournaments across a year, half of them already played, so both halves of the page are in the measurement. The e2e evidence spec `public-pagination.spec.ts` gained a 500-row, ten-season fixture asserting only the selected season renders.
- [x] Remove invalid page-two captures of a two-item list and obsolete facet captures from unique-surface counts. Test compatibility routes separately. `tools/surface-capture.mjs`: `Discovery · Live & upcoming · requested page 2`, `Discovery · Entries open` and `Discovery · Completed tournaments · page 2` are deleted from the public inventory; `Discovery · Live & upcoming` / `Completed tournaments` / `All results search` become `Discovery · Season calendar`, `Discovery · Earlier this season` (`/e/#past`) and `Discovery · Search across seasons` (`/e/?q=Open&year=all#calendar`). The three retired URLs that must still ANSWER moved to a new `ENTRANT_COMPATIBILITY_SURFACES` list, captured at the end of the run and **counted apart**: `routeCoverage` gains `compatibilitySheets`, `canonicalDestinations`/`stateSheets` are computed over product surfaces only, and the book's cover says so. `docs/reference/surface-map.md` matches. `tools/tests/surface-capture-layout.test.mjs` is green, including `SURFACE_CAPTURE_INTEGRATION=1` (3/3).

**Done when:** a reader can browse a season continuously, spot live events, enter an upcoming tournament, and reach past results without changing lifecycle tabs.

## P6 — Overview, Draws index, and Regulations

**Start in:** `app/routes/tournament.tsx`, `components/SectionCard.tsx`, `EventRow.tsx`, `app/routes/regulations.tsx`, `public/assets/regulations.js`, and public content models.

### Overview

- [x] Put the description in an **About** card spanning the full left column. — `tournament.tsx` OverviewPanel is now a two-column grid whose left column opens with `SectionCard title="About"` + new `SectionProse`; `tournament.render.test.ts` "Overview: About card…" asserts `>About<` + the organizer text.
- [x] Remove the small Events/Tournament time facts card; the Draws tab and shared local-time note cover those facts. — the `Events` / `Entered so far` `dl` is deleted; the same test asserts neither `>Events<` nor `Entered so far` renders.
- [x] Omit Pricing when no fees are published. Omit Documents when there are no documents; remove negative-state filler rows. — the Fees card renders only while an event is open (the "Fees are not published for this closed tournament" row is gone); Documents already renders only with regulations. Tests: "omits the pricing card entirely once no event is open" and "renders no document row when the director wrote no regulations".
- [x] Show currently relevant key dates. Remove long lists of elapsed opening/deadline timestamps from live/completed events. — `timelineModel` rewritten: one Entries row (never the elapsed opening date), a withdrawal deadline only while ahead, and nothing but Play once the play day is past. `phase.test.ts` "timelineModel (public-visual-fixes P6…)" pins all four states.
- [x] Use concise status context such as **Entries closed 22 Jul**, and label play dates **Play** or **Date** rather than Tournament. — moments carry `status` (`Opens`/`Closes`/`Closed`) rendered with P5's `formatDayMonthInZone` (venue-local, no zone spelling); the play row is labelled `Play`. Tests assert `>Entries<`/`>Play<` and no `>Tournament<`, and no `UTC`/`GMT±` in the document.

### Draws index

- [x] Reduce each row to **Event name · Entrants · Progress · Open**. — `EventRow.tsx` rewritten to four cells with `EVENT_ROW_COLUMNS` shared with the index header (`Event · Entrants · Progress`); `tournament.render.test.ts` "Draws: four cells per row" and the `components.test.ts` EventRow suite.
- [x] Remove duplicated pair counts, Open to all, derivable round counts, repeated format descriptions, and “Draw published · rounds to be scheduled”. — the whole facts line is gone; `components.test.ts` "drops the derivable draw description the index used to repeat" asserts none of `4 rounds` / `with consolation` / `Open to all` / `Draw published` / `Elimination`.
- [x] Show format only when it varies and helps distinguish rows. — `DrawsPanel` sets `showFormat` only when the published cards hold more than one `kind`; `components.test.ts` "shows the format only when the index says the formats differ".
- [x] Show truthful progress such as **R16 in play**, **Final 14:00**, or **Complete** from actual public data. — new backend `DrawProgressDTO` (`entries_site.py` `_draw_progress`) publishes the earliest unfinished round, its state and its venue-local start, results-gated; `drawProgressLabel` renders it. Tests: `test_entries_site_api.py::test_draw_progress_states_where_play_has_reached` (in_play/to_play/scheduled 11:00/complete, and `None` while results are unpublished) + the `components.test.ts` table.
- [x] Make the row a single clear native link with keyboard focus. Remove the misleading STATE heading above action buttons and avoid nested links. — the row is one `<a>` with `focus-visible:ring-accent`; the Entrants button and the champion PersonRef link are gone (champion text comes from the `personRefModel` seam), and the header column is `Progress`, not `State`. `components.test.ts` "is ONE native link per row…" counts exactly one `<a>`.
- [x] Treat `?tab=events`, `?tab=draws`, `?tab=seeds`, and `?tab=winners` as one Draws surface plus three aliases in coverage. Keep legacy destinations working. — `legacyDrawsTab` + a loader 302 onto `?tab=draws` (they used to 404); the three URLs join `ENTRANT_COMPATIBILITY_SURFACES` in `tools/surface-capture.mjs` so `routeCoverage.compatibilitySheets` counts them apart, and `docs/reference/surface-map.md` says so. `tools/tests/surface-capture-layout.test.mjs` stays green; redirect tests in `tournament.render.test.ts` and `draw.render.test.ts`.

### Regulations

- [x] Render regulations below the shared tournament frame; remove the separate organizer-document eyebrow and redundant page furniture. — the reader's title band is one `h2` (`Regulations`) plus the version line; the duplicate `Tournament regulations` heading and the sr-only `Regulations document` heading are gone and sections moved h4→h3. `myEntries.render.test.ts` asserts `>Regulations<`.
- [x] Connect operator-authored regulations text from operator-visual-fixes.md to this page. Share the existing content model rather than creating a second source. — verified one source end to end: console `SetupProduct.tsx` writes `public-info.regulationsText` → `entry_pages.regulations_text` (`db/models.py`) → `PageDTO.regulationsText` (`core/schemas.py`) → this route's loader. No new field, table or route was added.
- [x] Render supported headings, paragraphs, and lists with readable spacing. Escape/sanitize content and retain meaningful links; do not blindly render uploaded HTML. — new `parseRegulationBlocks` (paragraphs on blank lines, `-`/`*`/`•` bullets as `<ul>`), generalised link rendering for any http(s) address with a readable label, and no `dangerouslySetInnerHTML` anywhere. `regulations.unit.test.ts` (blocks) and `myEntries.render.test.ts` "escapes authored markup instead of rendering it, and links a bare address".
- [x] Keep **Print** and rename **Download text** to **Download**. Use a truthful text download unless a real PDF export is implemented; never label text output Save as PDF. — the button reads `Download` and still saves `tournament-regulations.txt` (`public/assets/regulations.js`, unchanged behaviour); the render test asserts `>Download<`, no `Download text`, and no `save as pdf` copy.
- [x] Ensure print output focuses on the document and necessary tournament identity, without navigation chrome or clipped content. — new page-scoped `apps/entrant/public/assets/regulations-print.css` (linked `media="print"` from this route only) hides the shell header/footer, the hero, every `nav` and the action buttons, drops the measure/padding caps, sets `overflow: visible`, keeps headings with their sections and prints link targets; the document carries a print-only tournament/date/venue identity line. Not visually verified on paper — no browser in this sandbox (see blockers).
- [x] Preserve reader access/publication rules and useful missing-document behavior. — the loader is untouched: no credential, uniform 404 for an unknown slug, a closed page and a tournament with no regulations text; the Documents tab and Overview row still appear only when text exists. `myEntries.render.test.ts` 404 cases and `tournament.render.test.ts` "renders no document row…" all still pass.

**Done when:** the Overview shows useful content, Draws rows are concise and informative, and regulations read as a document within the same tournament site.

## P7 — forms, button styles, local time, and copy

**Start in:** shared UI/format helpers, changed routes, `app/lib/format.ts`, existing page-scoped scripts, and shared design-system styles.

- [x] Remove visible **Find** and **Apply** submit buttons beside search fields. Use a search icon and a visually hidden, accessible submit control. New `apps/entrant/app/components/SearchField.tsx` (icon + `sr-only` label + `sr-only` submit, `SEARCH_SHELL`/`SEARCH_INPUT` in `lib/ui.ts`) is the ONE treatment, used by Players (`EntrantsList`), Schedule, the draw finder and Discovery; the Schedule "Apply" button and the draw "Find" button are gone. (`schedule.test.ts` "has no visible Apply or Find button", `discovery.render.test.ts` "searches with an icon…".)
- [x] Keep real form labels available to assistive technology; remove redundant visible “Find a player” labels when the compact field treatment already identifies the task. Every field keeps a real `<label for>` (`Find a player`, `Search matches by player`, `Day`, `Event`, `Court`, `Status`) rendered `sr-only`; `entrants-filter.js` no longer mints a visible label above the box. (`components.test.ts` "searches natively…".)
- [x] Support Enter submission with native GET forms. Keep field values and other active filters in the URL. Players searches by `?q=` **filtered on the server** (`tournament.tsx` loader → `EntrantsList`), carrying `tab=players`; Schedule's row is one GET form carrying `organization`/`day` as hidden fields. (`tournament.render.test.ts` "the directory search is a native GET that keeps the tab", `schedule.test.ts` "submits natively on Enter…".)
- [x] Make schedule day/view changes usable without JavaScript through links or native fallbacks; use a small change-submit enhancement where appropriate. Day and By time/By court stay links; a long or gappy day run becomes a native `<select name="day">` applied by the form's `sr-only` submit. New page-scoped `public/assets/schedule-filters.js` submits on select change and does nothing where `requestSubmit` is absent. (`scheduleFilters.script.test.ts`, `schedule.test.ts` "changes the day and the view without JavaScript" / "falls back to a native day SELECT".)
- [x] Combine Schedule controls into one sticky row: **day selector · By time / By court · search**. Retain other necessary filters compactly without restoring two stacked cards. `ScheduleControls` is one sticky band (`TOOLBAR_OFFSET`, the constant `EntrantsList` already used) holding day · organisation · search, with event/court/status folded into a `More filters` disclosure — the `LIST_CARD` wrapper and the four-column filter grid are deleted. (`schedule.test.ts` "puts the day, the organisation and the search in ONE sticky row".)
- [x] Remove the extra Schedule explanatory sentence and redundant headings. Keep result count/empty-state feedback concise. The `h2 Schedule` and “Find matches by day, time, or court.” are gone (`<main aria-label="Schedule">` keeps the accessible name); the empty state is now “Try another day, or clear a filter.” and the count stays `N matches [· page x of y]`.
- [x] Standardize **primary / secondary / link** styling. Remove decorative trailing arrows from Follow live, Draws & results, Results, View, and completed-tournament links. `lib/ui.ts` gains `ACTION_SECONDARY` / `ACTION_LINK` / `ACTION_LINK_BASE` / `ACTION_LINK_MUTED` (primary stays the design-system `Button variant="brand"`); arrows removed from `SeasonStatusCell` (Follow live, Results), `NowStrip` (Draws & results) and schedule pagination; `View`, `Clear path`, `Show …` moved onto `ACTION_LINK`. (`components.test.ts` "gives Follow live the same affordance as Results", `discovery.render.test.ts` "carries no decorative trailing arrow on any action".)
- [x] Use those same action styles for Print/Download and shared navigation controls. Regulations' Print and Download now wear `ACTION_SECONDARY` instead of a bespoke copy of it; `SegmentedNav` items gained the same visible focus outline the action registers carry (they had none).
- [x] Put **All times local to the venue** once in the tournament hero. Remove rendered GMT/UTC offsets and IANA identifiers such as Asia/Seoul from public page copy. Verified (P1's line, once per frame) and extended: `/e/{slug}/enter` states it once itself, since it is tournament-scoped, quotes a deadline and wears no frame. New assertion fails on `UTC`/`GMT±`/`KST`/`Asia/Seoul`/`+09:00` anywhere in the rendered body of every tournament-scoped route (`tournamentFrame.render.test.ts`). Account-tier pages (`my-entries`, `receipt`) deliberately keep an explicit zone: they span tournaments and carry no venue-time frame.
- [x] Convert actual instants using the event's timezone before display; removing a suffix is not a timezone fix. Keep timezone identifiers internally for correct computation. New `formatDayMonthTimeInZone` / `formatDateTimeInZone` / `formatInstantInZone` in `lib/format.ts`, all conversions. Fixed three raw renderings: the entry form's `Entry deadline` (printed the wire `… 23:59 UTC`) and `Tournament date` (raw `YYYY-MM-DD`), and `StickyTotalBar` (now takes `timeZone`). Schedule freshness delegates to the same authority instead of its own `Intl` (it was the tier's one `Sep 12, 2026, 7:35 PM`). `?? 'UTC'` guards every zone read so a zone-less projection can never render in the SSR node's local zone.
- [x] Use dates alone where sufficient; retain precise local times on deadlines, scheduled matches, and other time-sensitive details. Avoid duplicate timezone notes per card/page section. Play day and the regulations/updated lines stay dates; the Overview's Key dates deadline is now `Closes 1 Aug, 00:30` and the entry form's deadline `14 Aug 2026, 23:59`. Discovery keeps `Enter · closes 15 Aug` (day only) deliberately: it lists many venues and carries no venue-time note. No per-card note exists anywhere (test above counts the note exactly once per frame).
- [x] Validate midnight and cross-zone fixtures so conversion does not accidentally shift an intended date or deadline. Do not round a real deadline solely for appearance. `components.test.ts` → "venue-local instants (P7)": 15:30 UTC on 31 Jul is `1 Aug, 00:30` in Seoul and `31 Jul, 16:30` in London; 14:30 UTC is `1 Aug, 23:30` in Seoul (an evening match keeps its day); midnight lands on its own day; 23:59 stays 23:59 (`15 Aug 2026, 00:59` in BST) and is never rounded; unparseable → null, unknown zone → UTC rather than a dropped instant.
- [x] Audit all changed headings, rows, CTAs, placeholders, helper text, error states, empty states, breadcrumbs, and accessible names for internal jargon and generated prose. Read the rendered text of Schedule, Overview and Players end to end. Changes: the schedule `State` filter is `Status` / `Any status` (desk vocabulary), the empty state is one short sentence, `1 pairs` → `1 pair` (`entryCountLabel`, the pre-existing wart P6 logged), and `/e/{slug}/enter`'s floating `← Tournament name` is now the frame's breadcrumb trail (`Tournaments › Spring Open › Enter`, current segment plain text, no arrow). (`enter.render.test.ts` "speaks the frame's breadcrumb grammar".)
- [x] Keep muted past rows and feeder lines readable, focus visible, and primary player names in normal readable ink. A completed tournament's name is back in `text-foreground` (the past section is quiet through what it omits, not a greyed title); the bracket feeder line moved from `text-muted-foreground` to `text-text-secondary`; every action register and `SegmentedNav` item carries a `focus-visible` outline and the search shell a `focus-within` one. (`components.test.ts` "quiets a past row by what it drops".)

**Done when:** forms feel compact but still work natively, controls share one grammar, and dates/times are correct without technical timezone strings.

## P8 — acceptance and handoff

- [x] Verify every tournament route uses the same hero/tabs/breadcrumb composition and correct active state. Verified in a browser against the live fixture: seven tournament-scoped routes (overview, schedule, `?tab=draws`, `?tab=players`, a draw detail, a player profile, regulations) each render exactly one `h1#tournament-title`, the same five-tab bar `Overview · Schedule · Draws · Players · Documents`, `aria-current="page"` on the expected tab, a breadcrumb trail, and the venue-time note exactly once; `/e/{slug}/enter` renders the trail too. 36 assertions, all pass. Held by test: `apps/entrant/tests/tournamentFrame.render.test.ts` (P1's CASES table).
- [x] Verify Discovery → tournament → Players → profile/history → draw/schedule, with keyboard, touch, browser back, and JavaScript disabled. Walked in Chromium: a Discovery row opens the tournament; a Players row name is a link into the profile; the profile carries a history section; browser back returns to `?tab=players`; Tab from the skip link reaches a draw link in 11 stops and Enter opens it. Repeated in a `javaScriptEnabled: false` context — discovery, overview, Players, a profile, a draw, the schedule and regulations all answer 200 with a fully rendered `<main>`, and a Players row still navigates. Touch was not exercised: no touch device was available, and the mobile viewport (390×844) is captured and asserted instead.
- [x] Verify entry CTAs reach the proper tournament flow and preserve account/session continuation. The entries-open tournament page offers an `/e/{slug}/enter` CTA that lands on the enter route and serves a native form; the season calendar's row action reads `Enter · closes 2 Aug` and points at the same URL. Continuation itself is unchanged by this program and stays covered by `tools/public-login-continuation-check.mjs` and the `Account · Create account for tournament` sheet (`/e/signup?next=/e/{slug}/enter`) in the book.
- [x] Verify name links and path highlighting do not compete or break normal profile navigation. On the fixture's MS draw, 42 names inside the bracket are profile links; `?player={identity}` paints 4 nodes with `.is-person-path` server-side, the bracket still carries all 42 links, and clicking a name inside a highlighted path navigates to the profile. Held by test: `tests/e2e/tests/11-public-bracket-geometry.spec.ts` "a selected player path is painted server-side, with no JavaScript".
- [x] Verify court/code/score parity against operator records for singles, doubles, live, completed, retirement, and walkover fixtures. A parity probe compared the public projection with the operator's own `GET /tournaments/{id}/bracket`: all 155 public draw nodes map to an operator play unit; scores agree across 102 scored nodes and 1 walkover; all 155 references match the shared grammar (`R32·11` / `QF1` / `SF2` / `F`) and `Match n` appears nowhere; the schedule's 155 rows agree with the operator record and exercise every state (completed 101, scheduled 46, live 6, walkover 1, retired 1) across singles (47) and doubles (84). 18 checks, 0 failures.
- [x] Verify unknown feeders, private people, withheld scores, and disputed courts do not leak identifiers or unpublished data. Same probe: every empty draw slot is a declared feeder, bye or placeholder, and every feeder reference names a node inside the same published draw; the results-unpublished workspace publishes no score and no draw progress; the withheld and unknown person keys both answer 404 and the rendered 404 page does not echo the key back. Courts: all 6 live matches publish a court, one per court (1–6), and no non-live match publishes one. That last property is now a fixture gate — `tests/e2e/prepare-console-fixture.py::_assert_one_live_match_per_court`, pinned by four cases in `tests/e2e/test_console_fixture.py`.
- [x] Verify 16/32/larger supported draws, all rounds, long names, sticky headers, snap behavior, and 200% text zoom without clipped names or page-wide horizontal overflow. Browser, on the fixture's 32 draw at 1440px, 1440px with the root font at 32px (200% text) and 390px: page-wide horizontal overflow 0px in all three, all five round columns present, 0 clipped or truncated names, `.bracket-round-header` computes `position: sticky`, and the scroll region is `scroll-snap-type: x mandatory` at 390px and `none` on desktop. `npm --prefix tests/e2e run test:bracket-geometry` passes 9/9 in Chromium. **16 is covered by SSR render, not by a captured surface**: no fixture ships a draw smaller than 32, so `apps/entrant/tests/draw.render.test.ts` gained `renders every round of a %i draw, once` for 16 and 32 (columns, node count, long names, feeder slots); logged in the debt log.
- [x] Verify one-season listing order, year/search URLs, entry-state copy, past-row styling, and empty-season handling. One continuous season with `Upcoming tournaments` above and `Earlier this season` below, the `#past` anchor present, `?q=&year=all` answering 200 and finding the searched tournament, an empty season answering 200 with "No tournaments match", and all four retired queries (`?view=completed` → `/e/#past`, `?view=open`, `?page=2`, `?preset=week`) canonicalising. **Entry-state copy needed a second server**: the fixture's dates are frozen in the API but the entrant tier reads the real clock, so every row is "past" locally; against an instance with `Date` frozen at the fixture instant the open row reads `Enter · closes 2 Aug` and the past row offers Results. That clock split is logged in the debt log.
- [x] Verify venue-local dates across midnight and from browsers in another timezone. The schedule was rendered from three browser timezones (`UTC`, `America/Los_Angeles`, `Pacific/Kiritimati`, i.e. −7 to +14): one identical rendering, and no `UTC`, `GMT±`, offset, IANA id or zone abbreviation anywhere in the body. The Korea entry windows (`closesAt` 23:59 venue time, which is the previous or next calendar day in most of those zones) render in venue time with no zone spelled out from a +14 browser. Held by test: `tournamentFrame.render.test.ts`'s zone-leak assertion on every tournament route and `components.test.ts > venue-local instants (P7)`.
- [x] Verify regulations authoring → publication → readable body → print/download with valid content and links. Authored end to end: a new regulations document was `PUT` through the operator's `/tournaments/{id}/entry-page`, the version bumped 1 → 2, and the public reader published it as 8 paragraphs, a 3-item list, a `Version 2 · updated …` line, one print stylesheet linked `media="print"`, Print and Download controls, and the bare address `https://example.test/playing-code` rendered as one link with a readable label. Download fires a real download event in the browser; without script the page states the browser-print fallback instead.
- [x] Run focused entrant unit/SSR/script tests, changed backend projection tests, relevant browser geometry/journey checks, typecheck/build, and required repository gates. See the gate table in the handoff. `make check` exits 0 (console eslint / `tsc -b` / vitest / depcruise, entrant lint / typecheck / vitest / depcruise, ruff, import-linter 15/15, pytest, docs). One failure this program had caused was fixed here: `navCasingContract.test.ts` still read the entrant tab labels out of `TabBar.tsx` after P1 moved them to `lib/tournamentFrame.ts`, so it asserted over an empty match set. Browser: bracket geometry 9/9, the P8 journey harness 102/102, the parity probe 18/18. `tools/tests/*.mjs` 47 pass / 2 skipped.
- [x] Run the existing `apps/entrant/scripts/measure-page-weight.mjs` workflow on representative pages, including a full season, large Players list, and populated draw. Retain the existing budget; do not raise it to hide regressions. The 4 KB poster budget and the 8 KB entry budget are **unchanged and still pass** (`/e/` 2.8 KB on the 24-tournament season, `/e/spring-open` 2.2 KB, the enter page 8.1 KB and 8.7 KB at its 8-block ceiling). Three documents that had no number at all were added — Players at 256 long names (11.1 KB), a full 32 draw with every round populated (8.2 KB), and a schedule page (5.4 KB) — each referencing a different page-scoped script, so `entrants-filter.js`, `bracket-path.js` and P7's `schedule-filters.js` are now inside a gate. Their budget is a **separate, derived** `RESULTS_BUDGET_KB = 14`, on the script's own R8-F precedent; nothing about it touches the two existing ceilings.
- [x] Update route coverage to distinguish unique surfaces, legacy aliases, enhanced states, and legitimate errors. `tools/surface-capture.mjs` now reports four counts instead of two — `canonicalDestinations` 35, `stateSheets` 43, `enhancedStateSheets` 1, `expectedErrorSheets` 3, `compatibilitySheets` 8 — and the book's cover paragraph explains each. `?view=round` and `?view=path` moved from surfaces to compatibility (P4 made them aliases of the one bracket); the two player-refusal sheets and the unknown partner token became declared `Expected refusal · …` sheets; the server-painted player path became the enhanced-state sheet. A declared refusal's 404 no longer marks the run partial (`surface-capture.mjs`, `expectedError`), and `surface-capture-status.mjs` reports "declared refusal(s)" on its own line. `docs/reference/surface-map.md` documents the taxonomy.
- [x] Regenerate the public surface book with a successful profile capture, scored matches, populated later rounds, desktop and mobile states. Keep failure captures separate and remove duplicate alias pages. Clean book: `docs/screenshots/ui-review/public-p8-final/` — public entrant **complete, 55/55 sheets, 0 failed viewports, 4 declared refusals**, `fixtureMode: normal`; the operator book was regenerated alongside it (37/37, complete). `RESULTS_SLUG` is new: `SLUG` is the entries-open Korea workspace the account and entry sheets need, and every draw there is unplayed, so the content routes are now captured a second time against Taipei under `Results tournament · …` / `Results draw · …` — scored matches, a resolved later round, a champion, and a profile with cross-tournament history (`S43`, HTTP 200). Both viewports on every sheet. Failure book, captured separately on `FIXTURE_MODE=failure`: `docs/screenshots/ui-review/public-p8-failure/`. No alias page is counted as a surface; the eight compatibility sheets are labelled and tallied apart.
- [x] Complete the P0–P8 matrix with concise changed-file/test/capture evidence and any genuine blockers. Matrix ticked for P8; per-package changed files, gate results and open items are in `docs/audits/surface-book-remediation/public-handoff.md`, and five deferrals are logged in `docs/reference/debt-log.md`.

**Completion rule:** all requested changes are required except the critique's explicitly optional zoom/anchor embellishments. Existing partial components do not count as completion until the end-to-end experience matches this plan.

## Source anchors

The source review found reusable components and scripts, an existing tournament-person route, missing cross-tournament history in that DTO, and bracket height-ceiling assertions. These anchors identify where to work; they do not weaken the requirements above.

- [PlayShell](https://github.com/wongywrongy/ShuttleWorks/blob/997a6911e99f7019f2e566bb2b6d0a2378b3a9d0/apps/entrant/app/components/PlayShell.tsx)
- [PersonRef](https://github.com/wongywrongy/ShuttleWorks/blob/997a6911e99f7019f2e566bb2b6d0a2378b3a9d0/apps/entrant/app/components/PersonRef.tsx)
- [Player profile DTO](https://github.com/wongywrongy/ShuttleWorks/blob/997a6911e99f7019f2e566bb2b6d0a2378b3a9d0/apps/entrant/app/lib/player.types.ts)
- [MatchCard](https://github.com/wongywrongy/ShuttleWorks/blob/997a6911e99f7019f2e566bb2b6d0a2378b3a9d0/apps/entrant/app/components/MatchCard.tsx)
- [Draw route](https://github.com/wongywrongy/ShuttleWorks/blob/997a6911e99f7019f2e566bb2b6d0a2378b3a9d0/apps/entrant/app/routes/draw.tsx)
- [Existing bracket-path script](https://github.com/wongywrongy/ShuttleWorks/blob/997a6911e99f7019f2e566bb2b6d0a2378b3a9d0/apps/entrant/public/assets/bracket-path.js)
- [Discovery route](https://github.com/wongywrongy/ShuttleWorks/blob/997a6911e99f7019f2e566bb2b6d0a2378b3a9d0/apps/entrant/app/routes/discovery.tsx)
- [Schedule route](https://github.com/wongywrongy/ShuttleWorks/blob/997a6911e99f7019f2e566bb2b6d0a2378b3a9d0/apps/entrant/app/routes/schedule.tsx)
- [Bracket geometry tests to update](https://github.com/wongywrongy/ShuttleWorks/blob/997a6911e99f7019f2e566bb2b6d0a2378b3a9d0/tests/e2e/tests/11-public-bracket-geometry.spec.ts)
