# Historical analysis snapshots — NOT current truth

> **⚠️ SUPERSEDED / HISTORICAL.** The ownership maps, glossary, import-boundary
> notes, and design inventory in this directory are point-in-time analysis
> snapshots from the workspace-suite redesign (2026-06). They are kept for
> historical reference and may name files, paths, or module boundaries that have
> since changed — the SP-REFACTOR 1–6 cleanups relocated and deleted code after
> these were written.

For **current** architecture, use the VitePress docs site
(`docs/architecture/`, `docs/modules/`, `docs/contracts/`) and the authoritative
snapshot at `docs/audits/06-state-of-codebase.md`. This directory is excluded
from the built site (`srcExclude` in `docs/.vitepress/config.mts`).
